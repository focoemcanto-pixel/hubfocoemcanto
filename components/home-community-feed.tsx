'use client';

import Link from 'next/link';
import { useEffect, useRef, useState } from 'react';
import { Bookmark, CheckCircle2, Heart, Lock, MessageCircle, MoreHorizontal, Repeat2, Send, Sparkles, Trash2, Volume2, VolumeX, X } from 'lucide-react';

type FeedPost = { id: string; authorId?: string | null; authorName: string; authorAvatarUrl?: string | null; createdAt?: string | null; exerciseTitle?: string | null; exerciseSlug?: string | null; caption?: string | null; mediaUrl?: string | null; likesCount: number; commentsCount: number; canDelete?: boolean; isFollowing?: boolean; isLiked?: boolean; isSaved?: boolean; isReposted?: boolean; isVipAuthor?: boolean; isEvaluated?: boolean };
type FeedComment = { id?: string; authorName: string; text: string; createdAt?: string | null };
type Props = { initialPosts: FeedPost[]; hasVipAccess?: boolean; vipCheckoutUrl?: string };

const DEFAULT_VIP_CHECKOUT = 'https://pay.kiwify.com.br/HHr4eyM';
const textPostCss = `.home-post-media.text-post-media{aspect-ratio:auto;min-height:auto;background:transparent;border-top:1px solid rgba(255,255,255,.06);border-bottom:1px solid rgba(255,255,255,.06);padding:18px}.community-text-card{position:relative;overflow:hidden;border:1px solid rgba(245,199,107,.18);border-radius:28px;background:radial-gradient(circle at 70% 5%,rgba(245,199,107,.24),transparent 34%),linear-gradient(145deg,rgba(245,199,107,.13),rgba(255,255,255,.035) 44%,rgba(0,0,0,.32));padding:24px 22px;box-shadow:inset 0 1px 0 rgba(255,255,255,.08),0 22px 60px rgba(0,0,0,.25)}.community-text-card:before{content:'“';position:absolute;right:18px;top:-26px;font-family:Georgia,'Times New Roman',serif;font-size:120px;line-height:1;color:rgba(245,199,107,.12)}.community-text-type{display:inline-flex;align-items:center;gap:8px;width:max-content;border:1px solid rgba(245,199,107,.34);border-radius:999px;background:rgba(245,199,107,.10);color:#f5c76b;padding:8px 11px;text-transform:uppercase;letter-spacing:.12em;font-size:11px;font-weight:1000;margin-bottom:16px}.community-text-main{position:relative;z-index:2;margin:0;white-space:pre-line;color:#fff;line-height:1.28;font-weight:850;letter-spacing:-.025em;font-size:clamp(24px,4.8vw,44px)}.community-text-main.medium{font-size:clamp(21px,3.8vw,34px);line-height:1.36}.community-text-main.long{font-size:18px;line-height:1.55;font-weight:720;letter-spacing:-.01em}.community-text-footer{position:relative;z-index:2;display:flex;align-items:center;gap:10px;margin-top:18px;color:rgba(255,255,255,.62);font-size:13px}.community-text-dot{width:7px;height:7px;border-radius:999px;background:#f5c76b}.instagram-bottom-caption.text-only-caption{display:none}.text-post-media+.instagram-action-row{padding-top:14px}.vip-verified-badge{display:inline-grid;place-items:center;width:18px;height:18px;border-radius:999px;margin-left:5px;color:#130d05;background:linear-gradient(180deg,#ffe39b,#e9b348);box-shadow:0 0 0 2px rgba(245,199,107,.12),0 6px 18px rgba(245,199,107,.18);vertical-align:-3px}.vip-verified-badge svg{width:13px;height:13px}.teacher-evaluated-ribbon{position:absolute;left:14px;top:14px;z-index:4;display:flex;align-items:center;gap:7px;border:1px solid rgba(245,199,107,.45);border-radius:999px;background:rgba(8,8,12,.72);backdrop-filter:blur(12px);color:#f5c76b;padding:8px 11px;font-size:11px;font-weight:1000;letter-spacing:.08em;text-transform:uppercase}.video-thumb-fallback{position:absolute;inset:0;z-index:0;display:grid;place-items:end start;padding:24px;background:radial-gradient(circle at 72% 28%,rgba(245,199,107,.24),transparent 32%),radial-gradient(circle at 20% 70%,rgba(245,199,107,.12),transparent 34%),linear-gradient(145deg,#17120b,#050509 56%,#000);overflow:hidden}.video-thumb-fallback:before{content:'';position:absolute;inset:0;background:linear-gradient(18deg,transparent 0,transparent 42%,rgba(245,199,107,.16) 43%,transparent 44%,transparent 58%,rgba(255,255,255,.09) 59%,transparent 60%);opacity:.65}.video-thumb-fallback span{position:relative;z-index:1;max-width:80%;font-family:Georgia,'Times New Roman',serif;font-size:clamp(30px,7vw,54px);line-height:.92;text-transform:uppercase;font-weight:900;color:#fff;text-shadow:0 8px 30px #000}.community-feed-video{position:relative;z-index:1;opacity:0;transition:opacity .22s ease;background:transparent!important}.community-feed-video.ready{opacity:1}.home-post-media.instagram-reel-media{background:linear-gradient(145deg,#17120b,#050509)!important}@media(max-width:520px){.home-post-media.text-post-media{padding:14px 0;border:0}.community-text-card{border-radius:24px;margin:0 0;padding:22px 18px}.community-text-main{font-size:clamp(25px,8vw,37px)}.community-text-main.medium{font-size:clamp(21px,6.2vw,30px)}.community-text-main.long{font-size:17px}.community-text-type{font-size:10px}.teacher-evaluated-ribbon{left:10px;top:10px;font-size:10px}.video-thumb-fallback{padding:22px}.video-thumb-fallback span{font-size:clamp(32px,10vw,58px)}}`;

function initials(name?: string | null) { return String(name || 'Aluno').trim().split(' ').slice(0, 2).map((part) => part[0]).join('').toUpperCase(); }
function timeAgo(value?: string | null) { if (!value) return 'agora'; const diff = Math.max(0, Date.now() - new Date(value).getTime()); const minutes = Math.floor(diff / 60000); if (minutes < 1) return 'agora'; if (minutes < 60) return `${minutes}min`; const hours = Math.floor(minutes / 60); if (hours < 24) return `${hours}h`; return `${Math.floor(hours / 24)}d`; }
function textClass(text?: string | null) { const length = String(text || '').trim().length; if (length > 520) return 'long'; if (length > 160) return 'medium'; return ''; }
function textKind(text?: string | null) { const value = String(text || '').toLowerCase(); if (value.includes('?') || value.includes('dúvida') || value.includes('duvida')) return '❓ Dúvida'; if (value.includes('consegui') || value.includes('vitória') || value.includes('vitoria') || value.includes('evolu')) return '🏆 Conquista'; if (value.includes('testemunho') || value.includes('sensacional')) return '✨ Reflexão'; return '📝 Texto'; }
function VerifiedBadge() { return <span className="vip-verified-badge" title="Assinante VIP"><CheckCircle2 aria-hidden size={13} /></span>; }

export function HomeCommunityFeed({ initialPosts, hasVipAccess = false, vipCheckoutUrl = DEFAULT_VIP_CHECKOUT }: Props) {
  const [posts, setPosts] = useState(initialPosts);
  const [liked, setLiked] = useState<Record<string, boolean>>(() => Object.fromEntries(initialPosts.map((p) => [p.id, !!p.isLiked])));
  const [saved, setSaved] = useState<Record<string, boolean>>(() => Object.fromEntries(initialPosts.map((p) => [p.id, !!p.isSaved])));
  const [reposted, setReposted] = useState<Record<string, boolean>>(() => Object.fromEntries(initialPosts.map((p) => [p.id, !!p.isReposted])));
  const [following, setFollowing] = useState<Record<string, boolean>>(() => Object.fromEntries(initialPosts.map((p) => [p.authorId || p.id, !!p.isFollowing])));
  const [comments, setComments] = useState<Record<string, FeedComment[]>>({});
  const [commentsLoaded, setCommentsLoaded] = useState<Record<string, boolean>>({});
  const [commentsLoading, setCommentsLoading] = useState<Record<string, boolean>>({});
  const [soundOn, setSoundOn] = useState<Record<string, boolean>>({});
  const [videoReady, setVideoReady] = useState<Record<string, boolean>>({});
  const [menu, setMenu] = useState<string | null>(null);
  const [sheet, setSheet] = useState<string | null>(null);
  const [toast, setToast] = useState('');
  const [removing, setRemoving] = useState<string | null>(null);
  const [vipModal, setVipModal] = useState(false);
  const refs = useRef<Record<string, HTMLVideoElement | null>>({});

  useEffect(() => { document.body.classList.toggle('comments-open', !!sheet || vipModal); return () => document.body.classList.remove('comments-open'); }, [sheet, vipModal]);
  useEffect(() => {
    const videos = Object.values(refs.current).filter(Boolean) as HTMLVideoElement[];
    const io = new IntersectionObserver((entries) => entries.forEach((entry) => {
      const video = entry.target as HTMLVideoElement;
      if (entry.isIntersecting) video.play().catch(() => undefined);
      else video.pause();
    }), { rootMargin: '420px 0px', threshold: .35 });
    videos.forEach((video) => io.observe(video));
    return () => io.disconnect();
  }, [posts]);
  useEffect(() => { if (sheet) loadComments(sheet); }, [sheet]);

  function markVideoReady(id: string) { setVideoReady((value) => value[id] ? value : { ...value, [id]: true }); }
  function markVideoWaiting(id: string) { setVideoReady((value) => value[id] ? { ...value, [id]: false } : value); }
  function notice(text: string) { setToast(text); window.setTimeout(() => setToast(''), 1700); }
  function openVipModal() { setMenu(null); setVipModal(true); }
  async function postForm(url: string, data: Record<string, string>) { const form = new FormData(); Object.entries(data).forEach(([k, v]) => form.set(k, v)); return fetch(url, { method: 'POST', body: form, headers: { accept: 'application/json' } }); }
  async function loadComments(postId: string) { if (commentsLoaded[postId] || commentsLoading[postId]) return; setCommentsLoading((value) => ({ ...value, [postId]: true })); const response = await fetch(`/api/community/comments?post_id=${encodeURIComponent(postId)}`, { headers: { accept: 'application/json' } }); const data = await response.json().catch(() => null); setCommentsLoading((value) => ({ ...value, [postId]: false })); if (!response.ok || !data?.ok) { notice(data?.detail || 'Não foi possível carregar comentários.'); return; } setComments((value) => ({ ...value, [postId]: data.comments || [] })); setCommentsLoaded((value) => ({ ...value, [postId]: true })); }
  async function likePost(id: string) { const previousLiked = liked[id]; const previousPosts = posts; const next = !previousLiked; setLiked((v) => ({ ...v, [id]: next })); setPosts((list) => list.map((p) => p.id === id ? { ...p, likesCount: Math.max(0, p.likesCount + (next ? 1 : -1)) } : p)); const r = await postForm('/api/community/likes', { post_id: id, liked: String(next) }); const data = await r.json().catch(() => null); if (!r.ok || !data?.ok) { setLiked((v) => ({ ...v, [id]: !!previousLiked })); setPosts(previousPosts); notice(data?.detail || 'Não foi possível curtir.'); return; } if (typeof data.likes_count === 'number') setPosts((list) => list.map((p) => p.id === id ? { ...p, likesCount: data.likes_count } : p)); }
  async function savePost(id: string) { const previousSaved = saved[id]; const next = !previousSaved; setSaved((v) => ({ ...v, [id]: next })); notice(next ? 'Salvo nos favoritos.' : 'Removido dos favoritos.'); const r = await postForm('/api/community/saves', { post_id: id, saved: String(next) }); if (!r.ok) { setSaved((v) => ({ ...v, [id]: !!previousSaved })); notice('Não foi possível salvar.'); } }
  async function repeatPost(id: string) { const previousRepost = reposted[id]; const next = !previousRepost; setReposted((v) => ({ ...v, [id]: next })); notice(next ? 'Repostado no seu perfil.' : 'Repost removido.'); const r = await postForm('/api/community/reposts', { post_id: id, reposted: String(next) }); if (!r.ok) { setReposted((v) => ({ ...v, [id]: !!previousRepost })); notice('Execute o SQL de reposts para ativar.'); } }
  async function followAuthor(authorId?: string | null) { if (!authorId) return; const previousFollow = following[authorId]; const next = !previousFollow; setFollowing((value) => ({ ...value, [authorId]: next })); const response = await postForm('/api/community/follows', { following_id: authorId, following: String(next) }); const data = await response.json().catch(() => null); if (!response.ok || !data?.ok) { setFollowing((value) => ({ ...value, [authorId]: !!previousFollow })); notice(data?.detail || 'Não foi possível seguir. Execute o SQL de follows.'); return; } if (data.self) { setFollowing((value) => ({ ...value, [authorId]: false })); notice('Esse é o seu próprio perfil.'); return; } const persisted = Boolean(data.following); setFollowing((value) => ({ ...value, [authorId]: persisted })); setPosts((list) => list.map((post) => post.authorId === authorId ? { ...post, isFollowing: persisted } : post)); notice(persisted ? 'Agora você está seguindo.' : 'Você deixou de seguir.'); }
  async function addComment(id: string, formEl: HTMLFormElement) { const input = formEl.elements.namedItem('comment') as HTMLInputElement | null; const text = input?.value.trim() || ''; if (!text) return; const previousPosts = posts; const previousComments = comments[id] || []; input!.value = ''; const tempComment = { id: `temp-${Date.now()}`, authorName: 'Você', text, createdAt: new Date().toISOString() }; setComments((value) => ({ ...value, [id]: [...previousComments, tempComment] })); setCommentsLoaded((value) => ({ ...value, [id]: true })); setPosts((list) => list.map((p) => p.id === id ? { ...p, commentsCount: p.commentsCount + 1 } : p)); const response = await postForm('/api/community/comments', { post_id: id, comment: text }); const data = await response.json().catch(() => null); if (!response.ok || !data?.ok) { setPosts(previousPosts); setComments((value) => ({ ...value, [id]: previousComments })); notice(data?.detail || 'Comentário não enviado. Execute o SQL de comentários.'); return; } setComments((value) => ({ ...value, [id]: [...previousComments, data.comment || tempComment] })); if (typeof data.comments_count === 'number') setPosts((list) => list.map((p) => p.id === id ? { ...p, commentsCount: data.comments_count } : p)); }
  async function deletePost(id: string) { if (!window.confirm('Excluir esta publicação?')) return; const previous = posts; setRemoving(id); setMenu(null); setPosts((list) => list.filter((p) => p.id !== id)); const response = await fetch(`/api/community/posts/${id}`, { method: 'DELETE', headers: { accept: 'application/json' } }); const data = await response.json().catch(() => null); if (!response.ok || !data?.ok) { setPosts(previous); notice(data?.detail || 'Não foi possível excluir.'); } else notice('Publicação excluída.'); setRemoving(null); }
  function sharePost(id: string) { const url = `${window.location.origin}/aluno/comunidade#post-${id}`; if (navigator.share) navigator.share({ title: 'Publicação Foco em Canto', url }).catch(() => undefined); else { navigator.clipboard?.writeText(url); notice('Link copiado.'); } }
  function toggleSound(id: string) { const video = refs.current[id]; const on = !soundOn[id]; setSoundOn((v) => ({ ...v, [id]: on })); if (video) { video.muted = !on; video.play().catch(() => undefined); } }

  if (!posts.length) return <div className="empty-community-feed"><h3>Nenhuma postagem real ainda.</h3><p>Quando os alunos publicarem exercícios na comunidade, eles aparecerão aqui automaticamente.</p><Link className="premium-button gold" href="/aluno/comunidade">Criar primeira postagem</Link></div>;
  const sheetPost = posts.find((p) => p.id === sheet) || null;

  return <>
    <style dangerouslySetInnerHTML={{ __html: textPostCss }} />
    <div className="home-insta-feed instagram-mobile-feed">{posts.map((post) => {
      const isTextOnly = !post.mediaUrl;
      const caption = post.caption || 'Compartilhou uma prática.';
      return <article className={`home-insta-post instagram-post-card ${isTextOnly ? 'text-only-post-card' : ''} ${removing === post.id ? 'post-removing' : ''}`} id={`post-${post.id}`} key={post.id}>
        {reposted[post.id] ? <div className="repost-ribbon"><Repeat2 size={14} /> Você repostou essa prática</div> : null}
        {post.isEvaluated ? <div className="teacher-evaluated-ribbon"><Sparkles size={13} /> Avaliado pelo professor</div> : null}
        <header className="home-post-head instagram-post-head"><div className="instagram-author-avatar">{post.authorAvatarUrl ? <img src={post.authorAvatarUrl} alt={post.authorName} /> : <span>{initials(post.authorName)}</span>}</div><div className="instagram-author-copy"><strong>{post.authorName}{post.isVipAuthor ? <VerifiedBadge /> : null}</strong><span>{timeAgo(post.createdAt)}{post.isVipAuthor ? ' · VIP' : ''}</span></div>{!post.canDelete ? <button className={`instagram-follow-button ${following[post.authorId || post.id] ? 'following' : ''}`} type="button" onClick={() => followAuthor(post.authorId)}>{following[post.authorId || post.id] ? 'Seguindo' : 'Seguir'}</button> : null}<div className="home-post-options"><button className="home-post-menu" type="button" onClick={() => setMenu(menu === post.id ? null : post.id)}><MoreHorizontal size={28} /></button>{menu === post.id ? <div className="post-options-popover instagram-options-sheet"><Link href={`/aluno/comunidade#post-${post.id}`}>Ver publicação</Link>{post.exerciseSlug ? hasVipAccess ? <Link href={`/aluno/aula/${post.exerciseSlug}`}>Ver aula vinculada</Link> : <button type="button" onClick={openVipModal}>Ver aula vinculada</button> : null}<button type="button" onClick={() => repeatPost(post.id)}>{reposted[post.id] ? 'Remover repost' : 'Repostar'}</button><button type="button" onClick={() => sharePost(post.id)}>Compartilhar/copiar link</button>{post.canDelete ? <button className="danger-option" type="button" onClick={() => deletePost(post.id)}><Trash2 size={18} /> Excluir publicação</button> : null}</div> : null}</div></header>
        <div className={`home-post-media instagram-reel-media ${isTextOnly ? 'text-post-media' : ''}`}>{post.mediaUrl ? <><div className="video-thumb-fallback"><span>{post.exerciseTitle || caption}</span></div><video ref={(node) => { refs.current[post.id] = node; }} className={`community-feed-video ${videoReady[post.id] ? 'ready' : ''}`} src={post.mediaUrl} muted={!soundOn[post.id]} loop playsInline preload="auto" onLoadedData={(event) => { markVideoReady(post.id); if (!soundOn[post.id]) event.currentTarget.play().catch(() => undefined); }} onCanPlay={() => markVideoReady(post.id)} onWaiting={() => markVideoWaiting(post.id)} onStalled={() => markVideoWaiting(post.id)} onError={() => markVideoWaiting(post.id)} /><button className="home-sound-toggle instagram-sound-toggle" type="button" onClick={() => toggleSound(post.id)}>{soundOn[post.id] ? <Volume2 size={20} /> : <VolumeX size={20} />}</button>{post.exerciseTitle ? <div className="instagram-music-chip">♪ {post.exerciseTitle}</div> : null}</> : <div className="community-text-card"><span className="community-text-type">{textKind(caption)}</span><p className={`community-text-main ${textClass(caption)}`}>{caption}</p><div className="community-text-footer"><span className="community-text-dot" /> Publicação da comunidade</div></div>}</div>
        <div className="home-icon-actions instagram-action-row"><div className="instagram-action-left"><button type="button" className={liked[post.id] ? 'liked reaction-pop' : ''} onClick={() => likePost(post.id)}><Heart size={30} fill={liked[post.id] ? 'currentColor' : 'none'} /></button><button type="button" onClick={() => setSheet(post.id)}><MessageCircle size={30} /></button><button type="button" onClick={() => sharePost(post.id)}><Send size={30} /></button><button type="button" className={reposted[post.id] ? 'active-action reaction-pop' : ''} onClick={() => repeatPost(post.id)}><Repeat2 size={29} /></button></div><button type="button" className={`save-button ${saved[post.id] ? 'active-action reaction-pop' : ''}`} onClick={() => savePost(post.id)}><Bookmark size={31} fill={saved[post.id] ? 'currentColor' : 'none'} /></button></div>
        <div className="instagram-engagement-line"><strong>{post.likesCount}</strong> curtidas</div>{isTextOnly ? null : <p className="home-post-caption instagram-bottom-caption"><strong>{post.authorName}{post.isVipAuthor ? <VerifiedBadge /> : null}</strong> {caption}</p>}<button type="button" className="instagram-view-comments" onClick={() => setSheet(post.id)}>Ver todos os {post.commentsCount} comentários</button>{(comments[post.id] || []).slice(-2).map((c, i) => <p className="home-local-comments" key={c.id || i}><strong>{c.authorName}</strong> {c.text}</p>)}
      </article>;
    })}</div>
    {toast ? <div className="instagram-toast"><CheckCircle2 size={17} /> {toast}</div> : null}
    {vipModal ? <div className="vip-lock-backdrop" onClick={() => setVipModal(false)}><section className="vip-lock-modal" onClick={(e) => e.stopPropagation()}><button className="vip-lock-close" type="button" onClick={() => setVipModal(false)}><X size={22} /></button><div className="vip-lock-icon"><Lock size={30} /></div><p className="eyebrow"><Sparkles size={14} /> Exclusivo VIP</p><h3>Essa aula é para assinantes da Sala de Atividades VIP</h3><p>Assine para acessar exercícios, gravar duetos, enviar atividades para avaliação, postar na comunidade, receber correções do professor e evoluir com muito mais direção.</p><ul><li>Exercícios guiados e duetos</li><li>Envio para avaliação</li><li>Publicações na comunidade</li><li>Correções e acompanhamento</li></ul><a className="vip-lock-cta" href={vipCheckoutUrl}>Assinar e desbloquear agora</a><button className="vip-lock-later" type="button" onClick={() => setVipModal(false)}>Agora não</button></section></div> : null}
    {sheetPost ? <div className="comments-sheet-backdrop" onClick={() => setSheet(null)}><section className="comments-sheet" onClick={(e) => e.stopPropagation()}><div className="comments-sheet-handle" /><header><h3>Comentários</h3><button type="button" onClick={() => setSheet(null)}><X size={28} /></button></header><div className="comments-sheet-list">{commentsLoading[sheetPost.id] ? <p className="empty-comment-text">Carregando comentários...</p> : (comments[sheetPost.id] || []).length ? comments[sheetPost.id].map((c, i) => <p key={c.id || i}><strong>{c.authorName}</strong> {c.text}</p>) : <p className="empty-comment-text">Seja o primeiro a comentar essa prática.</p>}</div><form className="comments-sheet-form" onSubmit={(e) => { e.preventDefault(); addComment(sheetPost.id, e.currentTarget); }}><input name="comment" placeholder="Adicionar comentário..." /><button type="submit">Publicar</button></form></section></div> : null}
  </>;
}
