'use client';

import Link from 'next/link';
import { FormEvent, useEffect, useMemo, useRef, useState } from 'react';
import { createPortal } from 'react-dom';
import { Bookmark, CheckCircle2, Heart, Lock, MessageCircle, MoreHorizontal, Send, Sparkles, Trash2, Volume2, VolumeX, X } from 'lucide-react';

type FeedPost = {
  id: string;
  authorId?: string | null;
  authorName: string;
  authorAvatarUrl?: string | null;
  createdAt?: string | null;
  exerciseTitle?: string | null;
  exerciseSlug?: string | null;
  caption?: string | null;
  mediaUrl?: string | null;
  posterUrl?: string | null;
  likesCount: number;
  commentsCount: number;
  canDelete?: boolean;
  isLiked?: boolean;
  isSaved?: boolean;
  isFollowing?: boolean;
  isVipAuthor?: boolean;
};

type Comment = { id: string; text: string; createdAt?: string | null; authorName: string; authorAvatarUrl?: string | null; pending?: boolean };
type Props = { initialPosts: FeedPost[]; hasVipAccess?: boolean; vipCheckoutUrl?: string; currentProfileId?: string | null };

const DEFAULT_VIP_CHECKOUT = 'https://pay.kiwify.com.br/HHr4eyM';
const SYSTEM_DUET_CAPTIONS = new Set(['minha prática do dueto.', 'minha pratica do dueto.', 'compartilhou uma prática.', 'compartilhou uma pratica.', 'prática vocal.', 'pratica vocal.', 'novo dueto.']);

const css = `
.home-insta-feed.instagram-mobile-feed,.home-insta-feed{display:grid!important;gap:24px!important;width:100%!important;max-width:720px!important;margin:0 auto!important}.home-insta-post.instagram-post-card,.instagram-post-card{display:block!important;position:relative!important;overflow:hidden!important;border:1px solid rgba(255,255,255,.12)!important;border-radius:24px!important;background:#09090d!important;color:#fff!important;box-shadow:0 20px 70px rgba(0,0,0,.34)!important}.home-post-head.instagram-post-head{display:flex!important;align-items:center!important;gap:10px!important;padding:12px 14px!important;background:#09090d!important;min-height:70px!important}.instagram-author-avatar{width:45px!important;height:45px!important;min-width:45px!important;border-radius:50%!important;padding:2px!important;background:linear-gradient(45deg,#f9ce34,#ee2a7b,#6228d7)!important;display:grid!important;place-items:center!important;overflow:hidden!important}.instagram-author-avatar img,.instagram-author-avatar span{width:100%!important;height:100%!important;border-radius:50%!important;display:grid!important;place-items:center!important;object-fit:cover!important;background:#15100a!important;border:2px solid #09090d!important;color:#f5c76b!important;font-weight:1000!important}.instagram-author-copy{min-width:0!important;flex:1!important}.instagram-author-copy-row{display:flex!important;align-items:center!important;gap:10px!important;min-width:0!important}.instagram-author-copy strong{display:flex!important;align-items:center!important;gap:4px!important;color:#fff!important;font-size:16px!important;line-height:1.05!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important}.instagram-author-copy span{display:block!important;margin-top:3px!important;color:rgba(255,255,255,.58)!important;font-size:13px!important}.community-follow-button{border:0!important;background:transparent!important;color:#f5c76b!important;font-weight:950!important;font-size:13px!important;padding:0 2px!important;cursor:pointer!important}.community-follow-button.following{color:rgba(255,255,255,.62)!important}.home-post-options{position:relative!important;flex:0 0 auto!important}.home-post-menu{width:40px!important;height:40px!important;border:0!important;border-radius:50%!important;background:transparent!important;color:#fff!important;display:grid!important;place-items:center!important;cursor:pointer!important}.post-options-popover{position:absolute!important;right:0!important;top:42px!important;z-index:80!important;width:245px!important;border:1px solid rgba(255,255,255,.14)!important;border-radius:20px!important;background:rgba(12,12,16,.98)!important;box-shadow:0 18px 60px rgba(0,0,0,.42)!important;overflow:hidden!important;backdrop-filter:blur(18px)!important}.post-options-popover a,.post-options-popover button{width:100%!important;min-height:48px!important;padding:0 16px!important;border:0!important;border-bottom:1px solid rgba(255,255,255,.08)!important;display:flex!important;align-items:center!important;gap:8px!important;background:transparent!important;color:#fff!important;font-size:14px!important;font-weight:850!important;text-align:left!important;cursor:pointer!important;text-decoration:none!important}.danger-option{color:#ff6b7a!important}.home-post-media.instagram-reel-media,.instagram-reel-media{position:relative!important;display:block!important;background:#000!important;border-top:1px solid rgba(255,255,255,.08)!important;border-bottom:1px solid rgba(255,255,255,.08)!important;overflow:hidden!important;aspect-ratio:auto!important}.home-post-media.text-post-media{aspect-ratio:auto!important;min-height:auto!important;background:transparent!important;padding:18px!important}.community-feed-video,.home-post-media video,.instagram-reel-media video{display:block!important;width:100%!important;height:auto!important;max-height:none!important;object-fit:contain!important;background:#000!important}.feed-poster{position:absolute!important;inset:0!important;width:100%!important;height:100%!important;object-fit:contain!important;background:#000!important;z-index:2!important;opacity:1!important;transition:opacity .18s ease!important}.feed-poster.is-hidden{opacity:0!important;pointer-events:none!important}.feed-loading-pill{position:absolute!important;left:12px!important;top:12px!important;z-index:3!important;border-radius:999px!important;background:rgba(0,0,0,.58)!important;color:#fff!important;padding:7px 10px!important;font-size:12px!important;font-weight:900!important;backdrop-filter:blur(10px)!important}.home-sound-toggle.instagram-sound-toggle{position:absolute!important;right:12px!important;bottom:12px!important;z-index:8!important;width:40px!important;height:40px!important;border:1px solid rgba(255,255,255,.18)!important;border-radius:50%!important;background:rgba(0,0,0,.62)!important;color:#fff!important;display:grid!important;place-items:center!important;backdrop-filter:blur(10px)!important}.instagram-music-chip{position:absolute!important;left:12px!important;bottom:12px!important;max-width:calc(100% - 80px)!important;border-radius:999px!important;background:rgba(0,0,0,.45)!important;backdrop-filter:blur(10px)!important;padding:8px 12px!important;color:#fff!important;font-size:12px!important;font-weight:850!important;white-space:nowrap!important;overflow:hidden!important;text-overflow:ellipsis!important;z-index:7!important}.home-icon-actions.instagram-action-row{padding:11px 14px 6px!important;display:flex!important;align-items:center!important;justify-content:space-between!important;gap:14px!important}.instagram-action-left{display:flex!important;align-items:center!important;gap:18px!important}.instagram-action-row button{width:31px!important;height:31px!important;border:0!important;background:transparent!important;color:#fff!important;display:grid!important;place-items:center!important;cursor:pointer!important;padding:0!important}.home-icon-actions button.liked{color:#ff3040!important}.home-icon-actions .active-action{color:#f5c76b!important}.instagram-engagement-line{padding:0 14px 7px!important;color:#fff!important;font-size:14px!important}.instagram-bottom-caption{padding:0 14px 4px!important;margin:0!important;font-size:15px!important;line-height:1.45!important;color:rgba(255,255,255,.9)!important}.instagram-bottom-caption strong{margin-right:6px!important;color:#fff!important}.instagram-view-comments{display:block!important;border:0!important;background:transparent!important;color:rgba(255,255,255,.52)!important;font-size:14px!important;padding:4px 14px 12px!important;text-align:left!important;cursor:pointer!important}.community-text-card{border:1px solid rgba(245,199,107,.18)!important;border-radius:28px!important;background:linear-gradient(145deg,rgba(245,199,107,.13),rgba(255,255,255,.035),rgba(0,0,0,.32))!important;padding:24px 22px!important}.community-text-type{display:inline-flex!important;border:1px solid rgba(245,199,107,.34)!important;border-radius:999px!important;background:rgba(245,199,107,.10)!important;color:#f5c76b!important;padding:8px 11px!important;text-transform:uppercase!important;font-size:11px!important;font-weight:1000!important;margin-bottom:16px!important}.community-text-main{margin:0!important;white-space:pre-line!important;color:#fff!important;line-height:1.35!important;font-weight:800!important;font-size:clamp(20px,4vw,36px)!important}.vip-verified-badge{display:inline-grid!important;place-items:center!important;width:18px!important;height:18px!important;border-radius:999px!important;margin-left:5px!important;color:#130d05!important;background:linear-gradient(180deg,#ffe39b,#e9b348)!important;vertical-align:-3px!important}.community-comment-sheet-backdrop{position:fixed!important;inset:0!important;z-index:2147483647!important;background:rgba(0,0,0,.58)!important;backdrop-filter:blur(5px)!important;display:grid!important;align-items:end!important}.community-comment-sheet{width:min(720px,100%)!important;max-height:min(82dvh,720px)!important;margin:0 auto!important;border:1px solid rgba(255,255,255,.14)!important;border-radius:30px 30px 0 0!important;background:linear-gradient(180deg,rgba(19,19,25,.985),rgba(8,8,12,.985))!important;box-shadow:0 -24px 90px rgba(0,0,0,.65)!important;padding:18px 18px calc(18px + env(safe-area-inset-bottom))!important;display:grid!important;grid-template-rows:auto minmax(0,1fr) auto!important;gap:14px!important}.comment-sheet-head{display:flex!important;align-items:center!important;justify-content:space-between!important}.comment-sheet-head h3{margin:0!important;font-size:clamp(26px,6vw,42px)!important}.comment-sheet-close{border:0!important;background:rgba(255,255,255,.08)!important;color:#fff!important;border-radius:999px!important;width:52px!important;height:52px!important;display:grid!important;place-items:center!important}.comment-list{display:grid!important;align-content:start!important;gap:12px!important;overflow-y:auto!important;min-height:180px!important}.comment-row{display:grid!important;grid-template-columns:38px 1fr!important;gap:10px!important}.comment-avatar{width:38px!important;height:38px!important;border-radius:999px!important;display:grid!important;place-items:center!important;background:linear-gradient(180deg,#ffe39b,#e9b348)!important;color:#130d05!important;font-weight:950!important;overflow:hidden!important}.comment-avatar img{width:100%!important;height:100%!important;object-fit:cover!important}.comment-bubble{border:1px solid rgba(255,255,255,.10)!important;border-radius:18px!important;background:rgba(255,255,255,.055)!important;padding:10px 12px!important}.comment-bubble strong{display:block!important;font-size:14px!important}.comment-bubble p{margin:4px 0 0!important;color:rgba(255,255,255,.84)!important;line-height:1.35!important}.comment-row.pending{opacity:.72!important}.comment-form{display:grid!important;grid-template-columns:1fr auto!important;gap:10px!important}.comment-form input{height:50px!important;border:1px solid rgba(255,255,255,.14)!important;border-radius:999px!important;background:rgba(255,255,255,.07)!important;color:#fff!important;padding:0 16px!important;font:inherit!important;outline:0!important}.comment-form button{height:50px!important;border:0!important;border-radius:999px!important;background:linear-gradient(180deg,#ffe39b,#e9b348)!important;color:#130d05!important;font-weight:950!important;padding:0 18px!important}.comment-empty{color:rgba(255,255,255,.66)!important;text-align:center!important;padding:30px 12px!important;font-size:18px!important}.instagram-toast{position:fixed!important;left:50%!important;bottom:92px!important;transform:translateX(-50%)!important;z-index:2147483647!important;border:1px solid rgba(245,199,107,.28)!important;border-radius:999px!important;background:rgba(14,14,18,.95)!important;color:#fff!important;padding:12px 16px!important;display:flex!important;gap:8px!important}.vip-lock-backdrop{position:fixed!important;inset:0!important;z-index:2147483646!important;background:rgba(0,0,0,.68)!important;display:grid!important;place-items:center!important;padding:18px!important}.vip-lock-modal{position:relative!important;max-width:420px!important;border:1px solid rgba(245,199,107,.28)!important;border-radius:28px!important;background:linear-gradient(180deg,#17171d,#09090c)!important;padding:28px!important;color:#fff!important}.vip-lock-close{position:absolute!important;right:12px!important;top:12px!important;border:0!important;background:rgba(255,255,255,.08)!important;color:#fff!important;border-radius:999px!important;width:38px!important;height:38px!important}.vip-lock-cta{display:block!important;margin-top:18px!important;border-radius:16px!important;background:linear-gradient(180deg,#ffe39b,#e9b348)!important;color:#130d05!important;text-align:center!important;text-decoration:none!important;font-weight:950!important;padding:14px!important}@media(max-width:768px){.home-insta-feed.instagram-mobile-feed{gap:14px!important;margin-left:calc(50% - 50vw)!important;margin-right:calc(50% - 50vw)!important;width:100vw!important;max-width:100vw!important}.instagram-post-card{border-left:0!important;border-right:0!important;border-radius:0!important}.comment-form{grid-template-columns:1fr!important}.comment-form button{width:100%!important}}
`;

function initials(name?: string | null) { return String(name || 'Aluno').trim().split(' ').slice(0, 2).map((part) => part[0]).join('').toUpperCase(); }
function cleanCaption(value?: string | null) { const text = String(value || '').trim(); return SYSTEM_DUET_CAPTIONS.has(text.toLowerCase()) ? '' : text; }
function timeAgo(value?: string | null) { if (!value) return 'agora'; const diff = Math.max(0, Date.now() - new Date(value).getTime()); const m = Math.floor(diff / 60000); if (m < 1) return 'agora'; if (m < 60) return `${m}min`; const h = Math.floor(m / 60); if (h < 24) return `${h}h`; return `${Math.floor(h / 24)}d`; }
function textKind(text?: string | null) { const value = String(text || '').toLowerCase(); if (value.includes('?') || value.includes('dúvida') || value.includes('duvida')) return '❓ Dúvida'; if (value.includes('consegui') || value.includes('vitória') || value.includes('vitoria') || value.includes('evolu')) return '🏆 Conquista'; return '📝 Texto'; }
function VerifiedBadge() { return <span className="vip-verified-badge" title="Assinante VIP"><CheckCircle2 aria-hidden size={13} /></span>; }

function CommentSheet(props: { post: FeedPost | null; comments: Comment[]; loading: boolean; text: string; sending: boolean; onClose: () => void; onTextChange: (value: string) => void; onSubmit: (event: FormEvent) => void }) {
  const [mounted, setMounted] = useState(false);
  useEffect(() => setMounted(true), []);
  useEffect(() => { if (!props.post) return; const previous = document.body.style.overflow; document.body.style.overflow = 'hidden'; return () => { document.body.style.overflow = previous; }; }, [props.post]);
  if (!mounted || !props.post) return null;
  return createPortal(<div className="community-comment-sheet-backdrop" onClick={props.onClose}><section className="community-comment-sheet" onClick={(event) => event.stopPropagation()}><div className="comment-sheet-head"><h3>Comentários</h3><button className="comment-sheet-close" type="button" onClick={props.onClose}><X size={28} /></button></div><div className="comment-list">{props.loading ? <p className="comment-empty">Carregando comentários...</p> : props.comments.length ? props.comments.map((comment) => <div className={`comment-row ${comment.pending ? 'pending' : ''}`} key={comment.id}><div className="comment-avatar">{comment.authorAvatarUrl ? <img src={comment.authorAvatarUrl} alt="" /> : initials(comment.authorName)}</div><div className="comment-bubble"><strong>{comment.authorName}{comment.pending ? ' · enviando' : ''}</strong><p>{comment.text}</p></div></div>) : <p className="comment-empty">Ainda não há comentários. Seja o primeiro.</p>}</div><form className="comment-form" onSubmit={props.onSubmit}><input value={props.text} onChange={(event) => props.onTextChange(event.target.value)} placeholder="Escreva um comentário..." disabled={props.sending} /><button type="submit" disabled={props.sending || !props.text.trim()}>{props.sending ? 'Enviando...' : 'Enviar'}</button></form></section></div>, document.body);
}

export function HomeCommunityFeed({ initialPosts, hasVipAccess = false, vipCheckoutUrl = DEFAULT_VIP_CHECKOUT, currentProfileId = null }: Props) {
  const [sessionProfileId, setSessionProfileId] = useState<string | null>(currentProfileId);
  const effectiveProfileId = currentProfileId || sessionProfileId;
  const normalizedPosts = useMemo(() => initialPosts.map((post) => ({ ...post, caption: cleanCaption(post.caption), canDelete: Boolean(post.canDelete || (effectiveProfileId && post.authorId === effectiveProfileId)) })), [initialPosts, effectiveProfileId]);
  const [posts, setPosts] = useState(normalizedPosts);
  const [liked, setLiked] = useState<Record<string, boolean>>(() => Object.fromEntries(normalizedPosts.map((p) => [p.id, !!p.isLiked])));
  const [saved, setSaved] = useState<Record<string, boolean>>(() => Object.fromEntries(normalizedPosts.map((p) => [p.id, !!p.isSaved])));
  const [following, setFollowing] = useState<Record<string, boolean>>(() => Object.fromEntries(normalizedPosts.map((p) => [String(p.authorId || ''), !!p.isFollowing]).filter(([id]) => id)));
  const [soundPostId, setSoundPostId] = useState<string | null>(null);
  const [readyVideos, setReadyVideos] = useState<Record<string, boolean>>({});
  const soundPostRef = useRef<string | null>(null);
  const videoRefs = useRef(new Map<string, HTMLVideoElement>());
  const [menu, setMenu] = useState<string | null>(null);
  const [toast, setToast] = useState('');
  const [vipModal, setVipModal] = useState(false);
  const [commentPost, setCommentPost] = useState<FeedPost | null>(null);
  const [comments, setComments] = useState<Comment[]>([]);
  const [commentCache, setCommentCache] = useState<Record<string, Comment[]>>({});
  const [commentText, setCommentText] = useState('');
  const [commentsLoading, setCommentsLoading] = useState(false);
  const [commentSending, setCommentSending] = useState(false);

  useEffect(() => setPosts(normalizedPosts), [normalizedPosts]);
  useEffect(() => { soundPostRef.current = soundPostId; videoRefs.current.forEach((video, id) => { video.muted = soundPostId !== id; }); }, [soundPostId]);
  useEffect(() => { if (currentProfileId) return; fetch('/api/community/session', { headers: { accept: 'application/json' } }).then((r) => r.ok ? r.json() : null).then((data) => { if (!data?.profile?.id) return; setSessionProfileId(String(data.profile.id)); const next: Record<string, boolean> = {}; (data.followingIds || []).forEach((id: string) => { next[String(id)] = true; }); setFollowing((value) => ({ ...value, ...next })); }).catch(() => null); }, [currentProfileId]);
  useEffect(() => {
    const urls = posts.filter((p) => p.mediaUrl).slice(0, 6).map((p) => p.mediaUrl as string);
    const links = urls.map((url) => { const link = document.createElement('link'); link.rel = 'preload'; link.as = 'video'; link.href = url; document.head.appendChild(link); return link; });
    window.requestAnimationFrame(() => videoRefs.current.forEach((video, id) => { const index = posts.findIndex((p) => p.id === id); if (index >= 0 && index < 6) { video.preload = 'auto'; try { video.load(); } catch {} } }));
    return () => links.forEach((link) => link.remove());
  }, [posts]);
  useEffect(() => {
    const items = Array.from(videoRefs.current.entries());
    if (!items.length || typeof IntersectionObserver === 'undefined') return;
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        const video = entry.target as HTMLVideoElement;
        const id = video.dataset.postId || '';
        if (entry.isIntersecting) {
          video.preload = 'auto';
          try { video.load(); } catch {}
        }
        if (entry.isIntersecting && entry.intersectionRatio >= 0.38) {
          videoRefs.current.forEach((other, otherId) => { if (otherId !== id) other.pause(); });
          video.muted = soundPostRef.current !== id;
          video.play().catch(() => undefined);
        } else if (!entry.isIntersecting || entry.intersectionRatio < 0.18) {
          video.pause();
        }
      });
    }, { rootMargin: '700px 0px 700px 0px', threshold: [0, 0.18, 0.38, 0.7, 1] });
    items.forEach(([, video]) => observer.observe(video));
    return () => observer.disconnect();
  }, [posts.length]);

  function notice(text: string) { setToast(text); window.setTimeout(() => setToast(''), 1800); }
  function closeComments() { setCommentPost(null); setCommentText(''); setCommentsLoading(false); setCommentSending(false); }
  async function postForm(url: string, data: Record<string, string>) { const form = new FormData(); Object.entries(data).forEach(([k, v]) => form.set(k, v)); return fetch(url, { method: 'POST', body: form, headers: { accept: 'application/json' } }); }
  async function likePost(id: string) { const previous = liked[id]; const next = !previous; setLiked((v) => ({ ...v, [id]: next })); setPosts((list) => list.map((p) => p.id === id ? { ...p, likesCount: Math.max(0, p.likesCount + (next ? 1 : -1)) } : p)); const r = await postForm('/api/community/likes', { post_id: id, liked: String(next) }); if (!r.ok) { setLiked((v) => ({ ...v, [id]: previous })); setPosts((list) => list.map((p) => p.id === id ? { ...p, likesCount: Math.max(0, p.likesCount + (next ? -1 : 1)) } : p)); notice('Não foi possível curtir.'); } }
  async function savePost(id: string) { const previous = saved[id]; const next = !previous; setSaved((v) => ({ ...v, [id]: next })); const r = await postForm('/api/community/saves', { post_id: id, saved: String(next) }); if (!r.ok) { setSaved((v) => ({ ...v, [id]: previous })); notice('Não foi possível salvar.'); } }
  async function followAuthor(authorId?: string | null) { if (!authorId || authorId === effectiveProfileId) return; const next = !following[authorId]; setFollowing((value) => ({ ...value, [authorId]: next })); const r = await postForm('/api/community/follows', { following_id: authorId, following: String(next) }); const data = await r.json().catch(() => null); if (!r.ok || data?.error) { setFollowing((value) => ({ ...value, [authorId]: !next })); notice(data?.detail || 'Não foi possível seguir.'); } }
  async function openComments(post: FeedPost) { setCommentPost(post); setCommentText(''); const cached = commentCache[post.id]; if (cached) { setComments(cached); return; } setComments([]); setCommentsLoading(true); const response = await fetch(`/api/community/comments?post_id=${encodeURIComponent(post.id)}`, { headers: { accept: 'application/json' } }); const data = await response.json().catch(() => null); if (response.ok && data?.comments) { setComments(data.comments); setCommentCache((cache) => ({ ...cache, [post.id]: data.comments })); } else notice('Não foi possível carregar comentários.'); setCommentsLoading(false); }
  async function submitComment(event: FormEvent) { event.preventDefault(); if (!commentPost || !commentText.trim() || commentSending) return; const postId = commentPost.id; const text = commentText.trim(); const tempId = `temp-${Date.now()}`; const tempComment: Comment = { id: tempId, text, authorName: 'Você', createdAt: new Date().toISOString(), pending: true }; setCommentSending(true); setCommentText(''); setComments((list) => [...list, tempComment]); setCommentCache((cache) => ({ ...cache, [postId]: [...(cache[postId] || []), tempComment] })); setPosts((list) => list.map((p) => p.id === postId ? { ...p, commentsCount: p.commentsCount + 1 } : p)); const r = await postForm('/api/community/comments', { post_id: postId, comment: text }); const data = await r.json().catch(() => null); if (!r.ok || !data?.ok) { setComments((list) => list.filter((c) => c.id !== tempId)); setCommentCache((cache) => ({ ...cache, [postId]: (cache[postId] || []).filter((c) => c.id !== tempId) })); setPosts((list) => list.map((p) => p.id === postId ? { ...p, commentsCount: Math.max(0, p.commentsCount - 1) } : p)); setCommentText(text); notice(data?.detail || 'Não foi possível comentar.'); setCommentSending(false); return; } const savedComment = data.comment as Comment; const replace = (list: Comment[]) => list.map((c) => c.id === tempId ? savedComment : c); setComments(replace); setCommentCache((cache) => ({ ...cache, [postId]: replace(cache[postId] || []) })); setPosts((list) => list.map((p) => p.id === postId ? { ...p, commentsCount: data.comments_count ?? p.commentsCount } : p)); setCommentSending(false); }
  async function deletePost(id: string) { const target = posts.find((post) => post.id === id); if (!target?.canDelete) { notice('Você só pode excluir suas próprias publicações.'); return; } if (!window.confirm('Excluir esta publicação?')) return; const previous = posts; setMenu(null); setPosts((list) => list.filter((p) => p.id !== id)); const response = await fetch(`/api/community/posts/${id}`, { method: 'DELETE', headers: { accept: 'application/json' } }); const data = await response.json().catch(() => null); if (!response.ok || !data?.ok) { setPosts(previous); notice(data?.detail || data?.error || 'Não foi possível excluir.'); return; } notice('Publicação excluída.'); }
  function sharePost(id: string) { const url = `${window.location.origin}/aluno/comunidade#post-${id}`; navigator.clipboard?.writeText(url).then(() => notice('Link copiado.')).catch(() => notice('Copie o link pela barra do navegador.')); }
  function toggleSound(id: string) { const next = soundPostId === id ? null : id; setSoundPostId(next); videoRefs.current.forEach((video, videoId) => { video.muted = next !== videoId; if (videoId !== id) video.pause(); }); const video = videoRefs.current.get(id); if (video) { video.muted = next !== id; video.preload = 'auto'; video.play().catch(() => undefined); } }
  if (!posts.length) return <div className="empty-community-feed"><h3>Nenhuma postagem real ainda.</h3><p>Quando os alunos publicarem exercícios na comunidade, eles aparecerão aqui automaticamente.</p><Link className="premium-button gold" href="/aluno/comunidade">Criar primeira postagem</Link></div>;

  return <><style dangerouslySetInnerHTML={{ __html: css }} /><div className="home-insta-feed instagram-mobile-feed">{posts.map((post, index) => { const isTextOnly = !post.mediaUrl; const caption = cleanCaption(post.caption); const canFollow = Boolean(post.authorId && post.authorId !== effectiveProfileId); const soundOn = soundPostId === post.id; const isReady = readyVideos[post.id]; return <article className={`home-insta-post instagram-post-card ${isTextOnly ? 'text-only-post-card' : ''}`} id={`post-${post.id}`} key={post.id}><header className="home-post-head instagram-post-head"><div className="instagram-author-avatar">{post.authorAvatarUrl ? <img src={post.authorAvatarUrl} alt={post.authorName} loading="lazy" /> : <span>{initials(post.authorName)}</span>}</div><div className="instagram-author-copy"><div className="instagram-author-copy-row"><strong>{post.authorName}{post.isVipAuthor ? <VerifiedBadge /> : null}</strong>{canFollow ? <button className={`community-follow-button ${following[String(post.authorId)] ? 'following' : ''}`} type="button" onClick={() => followAuthor(post.authorId)}>{following[String(post.authorId)] ? 'Seguindo' : 'Seguir'}</button> : null}</div><span>{timeAgo(post.createdAt)}{post.isVipAuthor ? ' · VIP' : ''}</span></div><div className="home-post-options"><button className="home-post-menu" type="button" onClick={() => setMenu(menu === post.id ? null : post.id)}><MoreHorizontal size={28} /></button>{menu === post.id ? <div className="post-options-popover instagram-options-sheet"><Link href={`/aluno/comunidade#post-${post.id}`}>Ver publicação</Link>{post.exerciseSlug ? hasVipAccess ? <Link href={`/aluno/aula/${post.exerciseSlug}`}>Ver aula vinculada</Link> : <button type="button" onClick={() => setVipModal(true)}>Ver aula vinculada</button> : null}<button type="button" onClick={() => sharePost(post.id)}>Compartilhar/copiar link</button>{post.canDelete ? <button className="danger-option" type="button" onClick={() => deletePost(post.id)}><Trash2 size={18} /> Excluir publicação</button> : null}</div> : null}</div></header><div className={`home-post-media instagram-reel-media ${isTextOnly ? 'text-post-media' : ''}`}>{post.mediaUrl ? <>{post.posterUrl ? <img className={`feed-poster ${isReady ? 'is-hidden' : ''}`} src={post.posterUrl} alt="" loading={index < 3 ? 'eager' : 'lazy'} /> : !isReady ? <span className="feed-loading-pill">carregando vídeo...</span> : null}<video ref={(element) => { if (element) videoRefs.current.set(post.id, element); else videoRefs.current.delete(post.id); }} data-post-id={post.id} className="community-feed-video" src={post.mediaUrl} poster={post.posterUrl || undefined} muted={!soundOn} loop playsInline preload={index < 6 ? 'auto' : 'metadata'} controls={false} onLoadedData={() => setReadyVideos((value) => ({ ...value, [post.id]: true }))} onCanPlay={() => setReadyVideos((value) => ({ ...value, [post.id]: true }))} /> <button className="home-sound-toggle instagram-sound-toggle" type="button" onClick={() => toggleSound(post.id)}>{soundOn ? <Volume2 size={20} /> : <VolumeX size={20} />}</button>{post.exerciseTitle ? <div className="instagram-music-chip">♪ {post.exerciseTitle}</div> : null}</> : <div className="community-text-card"><span className="community-text-type">{textKind(caption)}</span><p className="community-text-main">{caption}</p></div>}</div><div className="home-icon-actions instagram-action-row"><div className="instagram-action-left"><button type="button" className={liked[post.id] ? 'liked reaction-pop' : ''} onClick={() => likePost(post.id)}><Heart size={30} fill={liked[post.id] ? 'currentColor' : 'none'} /></button><button type="button" onClick={() => openComments(post)}><MessageCircle size={30} /></button><button type="button" onClick={() => sharePost(post.id)}><Send size={30} /></button></div><button type="button" className={`save-button ${saved[post.id] ? 'active-action reaction-pop' : ''}`} onClick={() => savePost(post.id)}><Bookmark size={31} fill={saved[post.id] ? 'currentColor' : 'none'} /></button></div><div className="instagram-engagement-line"><strong>{post.likesCount}</strong> curtidas</div>{post.mediaUrl && caption ? <p className="home-post-caption instagram-bottom-caption"><strong>{post.authorName}{post.isVipAuthor ? <VerifiedBadge /> : null}</strong> {caption}</p> : null}<button type="button" className="instagram-view-comments" onClick={() => openComments(post)}><MessageCircle size={18} /> {post.commentsCount} comentários</button></article>; })}</div><CommentSheet post={commentPost} comments={comments} loading={commentsLoading} text={commentText} sending={commentSending} onClose={closeComments} onTextChange={setCommentText} onSubmit={submitComment} />{toast ? <div className="instagram-toast"><CheckCircle2 size={17} /> {toast}</div> : null}{vipModal ? <div className="vip-lock-backdrop" onClick={() => setVipModal(false)}><section className="vip-lock-modal" onClick={(e) => e.stopPropagation()}><button className="vip-lock-close" type="button" onClick={() => setVipModal(false)}><X size={22} /></button><div className="vip-lock-icon"><Lock size={30} /></div><p className="eyebrow"><Sparkles size={14} /> Exclusivo VIP</p><h3>Essa aula é para assinantes da Sala de Atividades VIP</h3><p>Assine para acessar exercícios, duetos e avaliações.</p><a className="vip-lock-cta" href={vipCheckoutUrl}>Assinar e desbloquear agora</a></section></div> : null}</>;
}
