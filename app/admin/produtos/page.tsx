import { revalidatePath } from 'next/cache';
import { redirect } from 'next/navigation';
import { AdminProductCreateModal } from '@/components/admin-product-create-modal';
import { createAdminClient } from '@/lib/supabase/admin';
import { dailyTrainingSteps, getDailyTrainingExercise, getExercisesByCategory, trainingCategories } from '@/lib/training-center';
import { accessLabel, getCentralAccessRows, saveCentralAccessRule, type CentralAccessLevel } from '@/lib/central-access';

export const dynamic = 'force-dynamic';

type ProductRow = { id: string; name: string; slug: string; description: string | null; type: string | null; status: string | null; cover_url: string | null; price_cents: number | null; billing_type: string | null; courses?: { id: string; slug: string | null; sort_order?: number | null }[] | null };

const PRODUCT_COVER_BUCKET = 'product-covers';
const defaultProducts = [
  { name: 'Grupo VIP', slug: 'grupo-vip', description: 'Sala de atividades, duetos, comunidade e análises do professor.', billing_type: 'recurring', type: 'subscription', status: 'published' },
  { name: 'Foco em Harmonia', slug: 'foco-em-harmonia', description: 'Curso completo para desenvolver percepção, divisão vocal e segunda voz.', billing_type: 'one_time', type: 'course', status: 'draft' },
  { name: 'Foco em Canto', slug: 'foco-em-canto', description: 'Técnica vocal, extensão, afinação e performance para cantar com controle.', billing_type: 'one_time', type: 'course', status: 'draft' },
  { name: 'Foco em Melismas', slug: 'foco-em-melismas', description: 'Agilidade vocal, riffs, runs e ornamentações para cantar com leveza.', billing_type: 'one_time', type: 'course', status: 'draft' },
  { name: 'Ebooks e Guias', slug: 'ebooks-e-guias', description: 'Materiais complementares para acelerar seus estudos vocais.', billing_type: 'one_time', type: 'course', status: 'draft' },
];

const css = `.admin-products-compact .admin-clean-hero{min-height:220px;padding:34px 36px}.admin-products-compact .admin-clean-hero h1{font-size:clamp(54px,7vw,86px);line-height:.88}.admin-products-compact .admin-course-grid{display:grid!important;grid-template-columns:repeat(auto-fill,minmax(340px,1fr))!important;gap:24px!important;align-items:stretch!important}.admin-products-compact .admin-course-card{min-height:0!important;height:auto!important;display:flex!important;flex-direction:column!important;border-radius:28px!important;overflow:visible!important;background:linear-gradient(180deg,rgba(255,255,255,.055),rgba(255,255,255,.024))!important}.admin-products-compact .admin-course-cover{aspect-ratio:16/10!important;height:auto!important;min-height:0!important;position:relative!important;overflow:hidden!important;border-radius:28px 28px 0 0!important;background:#09090d!important}.admin-products-compact .admin-course-cover img{width:100%!important;height:100%!important;object-fit:cover!important;display:block!important}.admin-products-compact .admin-course-cover span{height:100%;display:grid;place-items:center;font-size:38px;color:#f5c76b}.admin-products-compact .admin-course-body{flex:1!important;padding:18px 20px 20px!important;display:flex!important;flex-direction:column!important;gap:11px!important}.admin-products-compact .admin-course-body h2{font-size:26px!important;line-height:1.04!important;margin:0!important;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}.admin-products-compact .admin-course-body p{font-size:14px!important;line-height:1.38!important;margin:0!important;display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden}.product-meta-line{margin-top:auto!important;padding-top:12px!important}.admin-clean-actions{margin-top:6px!important;display:flex!important;gap:10px!important;flex-wrap:wrap!important}.admin-clean-button{padding:11px 16px!important;border-radius:14px!important}.product-manager-row{display:flex;gap:8px;flex-wrap:wrap;margin-top:2px}.product-manager-row form{display:inline-flex}.product-danger{border-color:rgba(255,95,95,.36)!important;color:#ffb4b4!important}.product-access-note{font-size:12px;color:rgba(255,255,255,.58)}.status-banner{margin:24px 0 0;border-radius:18px;padding:13px 16px;font-weight:950}.status-banner.good{border:1px solid rgba(81,227,138,.28);background:rgba(81,227,138,.08);color:#82ffad}.product-modal-toggle{display:none}.product-create-backdrop{position:fixed;inset:0;z-index:9999;display:none;place-items:center;background:rgba(0,0,0,.68);backdrop-filter:blur(16px);padding:26px}.product-modal-toggle:checked~.product-create-backdrop{display:grid}.product-create-modal{width:min(1120px,100%);max-height:calc(100dvh - 52px);overflow:auto;border:1px solid rgba(245,199,107,.22);border-radius:34px;background:radial-gradient(circle at 88% 0,rgba(245,199,107,.18),transparent 32%),linear-gradient(145deg,#15151b,#07080d);box-shadow:0 40px 140px rgba(0,0,0,.62);padding:30px;color:#fff}.product-modal-head{display:flex;justify-content:space-between;gap:18px;align-items:flex-start;margin-bottom:22px}.product-modal-head h2{font-size:42px;letter-spacing:-.05em;margin:4px 0 6px}.product-modal-head p{margin:0;color:rgba(255,255,255,.62)}.product-modal-close{width:44px;height:44px;border-radius:999px;border:1px solid rgba(255,255,255,.16);display:grid;place-items:center;color:#fff;background:rgba(255,255,255,.06);font-size:28px;cursor:pointer}.product-create-grid{display:grid;grid-template-columns:minmax(0,1fr) 360px;gap:22px}.product-create-form{display:grid;grid-template-columns:1fr 1fr;gap:16px}.product-create-form label{display:grid;gap:8px;color:rgba(255,255,255,.72);font-weight:850}.product-create-form input,.product-create-form select,.product-create-form textarea{width:100%;border:1px solid rgba(255,255,255,.12);border-radius:16px;background:rgba(255,255,255,.055);color:#fff;padding:15px 16px;font:inherit;outline:none}.product-create-form textarea{min-height:124px;resize:vertical}.product-create-form .wide{grid-column:1/-1}.product-create-form .submit{grid-column:1/-1;height:56px;border:0;border-radius:18px;background:linear-gradient(180deg,#ffe08a,#d59a2d);color:#120c04;font-weight:1000;font-size:16px}.product-cover-panel{border:1px solid rgba(245,199,107,.18);border-radius:26px;background:linear-gradient(145deg,rgba(245,199,107,.08),rgba(255,255,255,.025));padding:20px}.product-cover-panel .label{color:#f5c76b;text-transform:uppercase;letter-spacing:.18em;font-size:12px;font-weight:950}.product-cover-preview{margin:18px auto 18px;width:min(260px,100%);aspect-ratio:4/5;border:1px solid rgba(255,255,255,.12);border-radius:22px;background:radial-gradient(circle at 50% 20%,rgba(245,199,107,.28),transparent 34%),linear-gradient(145deg,#202026,#050609);display:grid;place-items:end center;overflow:hidden;padding:20px;text-align:center}.product-cover-preview.has-image{padding:0;place-items:stretch;background:#050609}.product-cover-preview img{width:100%;height:100%;object-fit:cover;display:block}.product-cover-preview strong{font-family:Georgia,'Times New Roman',serif;font-size:30px;line-height:.95;text-transform:uppercase}.product-cover-panel p{color:rgba(255,255,255,.62);line-height:1.45}.product-cover-upload{border:1px dashed rgba(245,199,107,.32);border-radius:20px;padding:16px;background:rgba(0,0,0,.16)}.product-card-mini-preview{display:grid;grid-template-columns:72px 1fr;gap:12px;align-items:center;border:1px solid rgba(255,255,255,.12);border-radius:18px;background:rgba(0,0,0,.22);padding:12px;margin-bottom:14px}.product-card-mini-preview .mini-cover{width:72px;height:90px;border-radius:13px;background:radial-gradient(circle at 50% 20%,rgba(245,199,107,.28),transparent 34%),linear-gradient(145deg,#202026,#050609);background-size:cover;background-position:center}.product-card-mini-preview strong{display:block;color:#fff;font-size:16px;line-height:1.05}.product-card-mini-preview p{font-size:12px!important;margin:6px 0 0!important;line-height:1.25!important;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden}.central-access-panel{margin:34px 0 0;border:1px solid rgba(245,199,107,.18);border-radius:30px;background:linear-gradient(135deg,rgba(255,255,255,.055),rgba(255,255,255,.02));padding:26px}.central-access-head{display:flex;align-items:flex-start;justify-content:space-between;gap:20px;margin-bottom:20px}.central-access-head h2{margin:0;font-size:34px;letter-spacing:-.04em}.central-access-head p{margin:8px 0 0;color:rgba(255,255,255,.62);max-width:720px}.central-access-grid{display:grid;gap:18px}.central-access-group{border:1px solid rgba(255,255,255,.09);border-radius:24px;background:rgba(0,0,0,.18);padding:18px}.central-access-group>h3{margin:0 0 14px;color:#f5c76b;text-transform:uppercase;letter-spacing:.12em;font-size:13px}.central-access-row{display:grid;grid-template-columns:minmax(0,1fr) 260px;gap:14px;align-items:center;border-top:1px solid rgba(255,255,255,.07);padding:14px 0}.central-access-row:first-of-type{border-top:0}.central-access-row strong{display:block;color:#fff;font-size:16px}.central-access-row small{display:block;margin-top:4px;color:rgba(255,255,255,.52);line-height:1.35}.central-access-row button{height:44px;border:0;border-radius:14px;background:#f5c76b;color:#130d04;font-weight:950;padding:0 12px}.central-access-control{display:grid;grid-template-columns:1fr auto;gap:8px}.access-segment{height:44px;display:grid;grid-template-columns:1fr 1fr;border:1px solid rgba(245,199,107,.22);border-radius:14px;overflow:hidden;background:#08090d}.access-segment label{display:grid;place-items:center;cursor:pointer;color:rgba(255,255,255,.62);font-weight:950;font-size:13px}.access-segment input{display:none}.access-segment label:has(input:checked){background:#f5c76b;color:#130d04}.level-pill{display:inline-flex;align-items:center;border:1px solid rgba(255,255,255,.12);border-radius:999px;padding:4px 8px;margin-left:8px;color:rgba(255,255,255,.68);font-size:11px;text-transform:uppercase}.level-pill.vip{border-color:rgba(245,199,107,.36);color:#f5c76b;background:rgba(245,199,107,.08)}.central-access-nested{margin-left:16px;padding-left:16px;border-left:1px solid rgba(245,199,107,.14)}@media(max-width:820px){.product-create-grid{grid-template-columns:1fr}.product-create-form{grid-template-columns:1fr}.product-create-form .wide,.product-create-form .submit{grid-column:auto}.central-access-row{grid-template-columns:1fr}.central-access-control{grid-template-columns:1fr}}@media(max-width:720px){.admin-products-compact .admin-clean-hero{padding:28px 22px}.admin-products-compact .admin-course-grid{grid-template-columns:1fr!important;gap:16px!important}}`;

function slugify(value: string) { return value.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '') || `produto-${Date.now()}`; }
function money(cents?: number | null) { return ((cents || 0) / 100).toLocaleString('pt-BR', { style: 'currency', currency: 'BRL' }); }
function orderOf(product: ProductRow) { return Number(product.courses?.[0]?.sort_order ?? 9999); }
function ruleMap(rows: { key: string; level: CentralAccessLevel }[]) { return Object.fromEntries(rows.map((row) => [row.key, row.level])) as Record<string, CentralAccessLevel>; }
function getRule(rules: Record<string, CentralAccessLevel>, key: string) { return rules[key] || 'open'; }
function normalizeUrl(value: string) { const raw = value.trim(); if (!raw) return ''; return /^https?:\/\//i.test(raw) ? raw : `https://${raw}`; }

async function safeInsertProduct(payload: Record<string, unknown>, fallback: Record<string, unknown>) { const supabase = createAdminClient(); const first = await supabase.from('products').insert(payload).select('id').single(); if (!first.error) return first; const message = String(first.error.message || '').toLowerCase(); if (message.includes('column') || message.includes('schema cache')) return supabase.from('products').insert(fallback).select('id').single(); return first; }
async function ensureCoverBucket() { const supabase = createAdminClient(); const { data: buckets } = await supabase.storage.listBuckets(); const exists = buckets?.some((bucket) => bucket.id === PRODUCT_COVER_BUCKET || bucket.name === PRODUCT_COVER_BUCKET); if (!exists) await supabase.storage.createBucket(PRODUCT_COVER_BUCKET, { public: true }); }
async function uploadCover(file: File | null, slug: string) { if (!file || file.size <= 0) return ''; const supabase = createAdminClient(); await ensureCoverBucket(); const ext = file.name.split('.').pop() || 'png'; const path = `${slug}-${Date.now()}.${ext}`; const { error } = await supabase.storage.from(PRODUCT_COVER_BUCKET).upload(path, file, { upsert: true, contentType: file.type || 'image/png' }); if (error) return ''; const { data } = supabase.storage.from(PRODUCT_COVER_BUCKET).getPublicUrl(path); return data.publicUrl || ''; }

async function getAdminProducts(): Promise<ProductRow[]> { const supabase = createAdminClient(); const { data, error } = await supabase.from('products').select('id,name,slug,description,type,status,cover_url,price_cents,billing_type,courses(id,slug,sort_order)').or('status.is.null,status.neq.archived').order('created_at', { ascending: true }); if (error) { console.error('Falha ao carregar produtos no Admin', error.message); return []; } return ((data || []) as ProductRow[]).sort((a, b) => orderOf(a) - orderOf(b)); }
async function ensureDefaultProducts() { const supabase = createAdminClient(); const { data: existing } = await supabase.from('products').select('id,slug').limit(1); if ((existing || []).length > 0) return; let order = 1; for (const item of defaultProducts) { const { data: product } = await supabase.from('products').insert({ ...item, price_cents: 0, cta_label: 'Acessar' }).select('id').single(); if (product?.id) await supabase.from('courses').insert({ product_id: product.id, title: item.name, slug: item.slug, subtitle: item.description.slice(0, 140), description: item.description, status: item.status, sort_order: order }); order += 1; } }
async function createProduct(formData: FormData) { 'use server'; const supabase = createAdminClient(); const name = String(formData.get('name') || '').trim(); const description = String(formData.get('description') || '').trim(); const billingType = String(formData.get('billing_type') || 'one_time'); const price = Number(formData.get('price') || 0); const status = String(formData.get('status') || 'draft') === 'published' ? 'published' : 'draft'; const type = String(formData.get('type') || (billingType === 'recurring' ? 'subscription' : 'course')); const slug = slugify(String(formData.get('slug') || name)); if (!name) redirect('/admin/produtos?created=0'); const file = formData.get('cover_file'); const uploadedCover = await uploadCover(file instanceof File ? file : null, slug); const coverUrl = uploadedCover || normalizeUrl(String(formData.get('cover_url') || '').trim()); const redirectUrl = normalizeUrl(String(formData.get('redirect_url') || '').trim()); const { data: last } = await supabase.from('courses').select('sort_order').order('sort_order', { ascending: false }).limit(1).maybeSingle(); const sortOrder = Number(last?.sort_order || 0) + 1; const basePayload = { name, slug, description, type, billing_type: billingType, price_cents: Math.round(price * 100), status, cta_label: 'Acessar', cover_url: coverUrl, redirect_url: redirectUrl, sales_page_url: redirectUrl, sales_url: redirectUrl, external_url: redirectUrl }; const fallbackPayload = { name, slug, description, type, billing_type: billingType, price_cents: Math.round(price * 100), status, cta_label: 'Acessar', cover_url: coverUrl }; const { data: product, error } = await safeInsertProduct(basePayload, fallbackPayload); if (!error && product?.id) { const baseCourse = { product_id: product.id, title: name, slug, subtitle: description.slice(0, 140), description, status, cover_url: coverUrl, sort_order: sortOrder, redirect_url: redirectUrl, sales_page_url: redirectUrl, sales_url: redirectUrl, external_url: redirectUrl }; const fallbackCourse = { product_id: product.id, title: name, slug, subtitle: description.slice(0, 140), description, status, cover_url: coverUrl, sort_order: sortOrder }; const course = await supabase.from('courses').insert(baseCourse); if (course.error) await supabase.from('courses').insert(fallbackCourse); } revalidatePath('/admin/produtos'); revalidatePath('/aluno'); revalidatePath('/aluno/biblioteca'); redirect(`/admin/produtos?created=${error ? '0' : '1'}`); }
async function setProductStatus(formData: FormData) { 'use server'; const supabase = createAdminClient(); const id = String(formData.get('id') || ''); const status = String(formData.get('status') || 'draft'); if (!id) return; await supabase.from('products').update({ status }).eq('id', id); await supabase.from('courses').update({ status }).eq('product_id', id); revalidatePath('/admin/produtos'); revalidatePath('/aluno'); revalidatePath('/aluno/biblioteca'); redirect('/admin/produtos?productStatus=1'); }
async function moveProduct(formData: FormData) { 'use server'; const supabase = createAdminClient(); const id = String(formData.get('id') || ''); const direction = String(formData.get('direction') || 'up'); const { data } = await supabase.from('products').select('id,courses(id,sort_order)').or('status.is.null,status.neq.archived'); const list = ((data || []) as ProductRow[]).filter((p) => p.courses?.[0]?.id).sort((a, b) => orderOf(a) - orderOf(b)); const index = list.findIndex((p) => p.id === id); const targetIndex = direction === 'down' ? index + 1 : index - 1; if (index < 0 || targetIndex < 0 || targetIndex >= list.length) return; const current = list[index]; const target = list[targetIndex]; await supabase.from('courses').update({ sort_order: orderOf(target) }).eq('id', current.courses?.[0]?.id); await supabase.from('courses').update({ sort_order: orderOf(current) }).eq('id', target.courses?.[0]?.id); revalidatePath('/admin/produtos'); revalidatePath('/aluno'); revalidatePath('/aluno/biblioteca'); redirect('/admin/produtos?order=1'); }
async function archiveProduct(formData: FormData) { 'use server'; const supabase = createAdminClient(); const id = String(formData.get('id') || ''); if (!id) return; await supabase.from('products').update({ status: 'archived' }).eq('id', id); await supabase.from('courses').update({ status: 'archived' }).eq('product_id', id); revalidatePath('/admin/produtos'); revalidatePath('/aluno'); revalidatePath('/aluno/biblioteca'); redirect('/admin/produtos?archived=1'); }
async function setCentralAccessRule(formData: FormData) { 'use server'; const key = String(formData.get('key') || '').trim(); const level = String(formData.get('level') || 'open') === 'vip' ? 'vip' : 'open'; const note = String(formData.get('note') || '').trim(); if (!key) redirect('/admin/produtos?centralSaved=0#central-access'); await saveCentralAccessRule(key, level, note); revalidatePath('/admin/produtos'); revalidatePath('/aluno/central'); revalidatePath('/aluno/central/diarios'); revalidatePath('/aluno/central/personalizado'); redirect(`/admin/produtos?centralSaved=${encodeURIComponent(key)}#central-access`); }

function AccessControl({ itemKey, title, description, rules }: { itemKey: string; title: string; description: string; rules: Record<string, CentralAccessLevel> }) { const value = getRule(rules, itemKey); return <form className="central-access-row" action={setCentralAccessRule}><div><strong>{title}<span className={`level-pill ${value === 'vip' ? 'vip' : ''}`}>{accessLabel(value)}</span></strong><small>{description}</small></div><div className="central-access-control"><input type="hidden" name="key" value={itemKey} /><input type="hidden" name="note" value={description} /><div className="access-segment"><label><input type="radio" name="level" value="open" defaultChecked={value === 'open'} />Aberto</label><label><input type="radio" name="level" value="vip" defaultChecked={value === 'vip'} />Grupo VIP</label></div><button type="submit">Salvar</button></div></form>; }

export default async function AdminProductsPage({ searchParams }: { searchParams?: Promise<Record<string, string | string[] | undefined>> }) {
  await ensureDefaultProducts();
  const resolvedSearchParams = await searchParams;
  const created = resolvedSearchParams?.created === '1';
  const changed = Boolean(resolvedSearchParams?.productStatus || resolvedSearchParams?.order || resolvedSearchParams?.archived);
  const centralSaved = Boolean(resolvedSearchParams?.centralSaved);
  const [products, accessRows] = await Promise.all([getAdminProducts(), getCentralAccessRows()]);
  const rules = ruleMap(accessRows as any);
  return (
    <main className="admin-page-clean admin-products-compact" id="top">
      <style dangerouslySetInnerHTML={{ __html: css }} />
      <input id="create-product-toggle" className="product-modal-toggle" type="checkbox" />
      <section className="admin-clean-hero"><div><span className="admin-clean-eyebrow">Escola Foco em Canto</span><h1>Produtos</h1><p>Crie produtos, publique na Home/Biblioteca, bloqueie com CTA VIP e controle a ordem dos cards.</p></div><label className="admin-clean-button primary" htmlFor="create-product-toggle">Criar produto</label></section>
      {changed ? <div className="status-banner good">Produto atualizado. A Home e a Biblioteca já foram revalidadas.</div> : null}{created ? <div className="status-banner good">Produto criado. Ele já aparece na Home/Biblioteca conforme o status escolhido.</div> : null}
      <section className="admin-course-grid">{products.map((product, index) => { const isPublished = product.status === 'published'; return <article className="admin-course-card" key={product.id}><div className="admin-course-cover">{product.cover_url ? <img src={product.cover_url} alt={product.name} /> : <span>{product.name.slice(0, 2).toUpperCase()}</span>}</div><div className="admin-course-body"><span className="admin-clean-pill">{product.billing_type === 'recurring' ? 'Assinatura' : 'Pagamento único'} · {isPublished ? 'liberado na home' : 'bloqueado/VIP'}</span><h2>{product.name}</h2><p className="admin-clean-muted">{product.description || 'Sem descrição cadastrada.'}</p><div className="product-meta-line"><strong>{money(product.price_cents)}</strong><small>Ordem {index + 1}</small></div><small className="product-access-note">Publicado libera o card na Home/Biblioteca. Rascunho mantém o card bloqueado com CTA.</small><div className="admin-clean-actions"><a className="admin-clean-button primary" href={`/admin/produtos/${product.id}`}>Gerenciar</a><a className="admin-clean-button secondary" href={`/admin/produtos/${product.id}/destino`}>Destino</a><a className="admin-clean-button secondary" href="/aluno">Prévia Home</a></div><div className="product-manager-row"><form action={moveProduct}><input type="hidden" name="id" value={product.id} /><input type="hidden" name="direction" value="up" /><button className="admin-clean-button secondary" disabled={index === 0}>↑</button></form><form action={moveProduct}><input type="hidden" name="id" value={product.id} /><input type="hidden" name="direction" value="down" /><button className="admin-clean-button secondary" disabled={index === products.length - 1}>↓</button></form><form action={setProductStatus}><input type="hidden" name="id" value={product.id} /><input type="hidden" name="status" value={isPublished ? 'draft' : 'published'} /><button className="admin-clean-button secondary">{isPublished ? 'Bloquear' : 'Liberar'}</button></form><form action={archiveProduct}><input type="hidden" name="id" value={product.id} /><button className="admin-clean-button secondary product-danger">Remover</button></form></div></div></article>; })}</section>
      <section id="central-access" className="central-access-panel">{centralSaved ? <div className="status-banner good">Permissão salva com sucesso.</div> : null}<div className="central-access-head"><div><span className="admin-clean-eyebrow">Controle granular</span><h2>Central de Atividades</h2><p>Controle a liberação externa e interna da Central. Cada item fica como Aberto ou exclusivo para assinantes do Grupo VIP.</p></div><a className="admin-clean-button secondary" href="/aluno/central">Prévia da Central</a></div><div className="central-access-grid"><div className="central-access-group"><h3>Entradas principais</h3><AccessControl itemKey="central" title="Central inteira" description="Controla o acesso geral à Central de Treinamento." rules={rules} /><AccessControl itemKey="daily" title="Exercícios Diários" description="Controla o card e a entrada dos desafios diários." rules={rules} /><AccessControl itemKey="personalized" title="Personalizados" description="Controla o card e a entrada do treino por objetivo." rules={rules} /><AccessControl itemKey="repertoire" title="Estude seu Repertório" description="Controla o card de estudo de repertório dentro da Central." rules={rules} /></div><div className="central-access-group"><h3>Diários · dias e exercícios internos</h3>{dailyTrainingSteps.map((step) => { const exercise = getDailyTrainingExercise(step); return <div className="central-access-nested" key={step.exerciseNumber}><AccessControl itemKey={`daily_step_${step.exerciseNumber}`} title={`Dia ${step.day} · Atividade ${step.exerciseNumber}`} description={step.title} rules={rules} />{exercise ? <AccessControl itemKey={`exercise_${exercise.slug}`} title={`Exercício interno · ${exercise.title}`} description={exercise.description} rules={rules} /> : null}</div>; })}</div><div className="central-access-group"><h3>Personalizados · categorias e exercícios</h3>{trainingCategories.map((category) => <div className="central-access-nested" key={category.slug}><AccessControl itemKey={`custom_category_${category.slug}`} title={category.title} description={category.description} rules={rules} />{getExercisesByCategory(category.slug).map((exercise) => <AccessControl itemKey={`exercise_${exercise.slug}`} title={exercise.title} description={exercise.description} rules={rules} key={exercise.slug} />)}</div>)}</div></div></section>
      <AdminProductCreateModal action={createProduct} />
    </main>
  );
}
