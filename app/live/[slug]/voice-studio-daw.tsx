'use client';

import { useEffect, useMemo, useRef, useState } from 'react';
import { AudioLines, ChevronDown, Circle, Copy, Download, KeyboardMusic, Magnet, Mic2, Pause, Play, Plus, Redo2, Scissors, Square, Trash2, Undo2, Volume2, ZoomIn, ZoomOut } from 'lucide-react';

type Status = 'idle' | 'countin' | 'recording' | 'playing';
type TrackKind = 'audio' | 'midi';
type MidiNote = { id:string; note:number; velocity:number; start:number; duration:number };
type Track = { id:string; kind:TrackKind; name:string; color:string; url?:string; blob?:Blob; start:number; sourceOffset:number; duration:number; sourceDuration:number; peaks:number[]; notes:MidiNote[]; instrument:string; muted:boolean; solo:boolean; volume:number };
type ArmedTrack = { kind:TrackKind; instrument:string };
type EditMode = 'move' | 'trim-left' | 'trim-right';
type DragState = { id:string; mode:EditMode; startX:number; initial:Track; timelineWidth:number };

const COLORS=['#22c55e','#8b5cf6','#0ea5e9','#f97316','#ec4899','#eab308'];
const BARS=16;
const MIN_CLIP=.08;
const EDITOR_CSS=`.vs-daw{--vs-zoom:1}.vs-edit-tools{display:flex;align-items:center;gap:4px}.vs-edit-tools button{width:30px;height:30px;border:1px solid #343946;border-radius:7px;background:#1b1f28;color:#aeb4c2;display:grid;place-items:center}.vs-edit-tools button svg{width:14px}.vs-edit-tools button.active{border-color:#8b5cf6;background:#2d2151;color:#ddd6fe}.vs-edit-tools button:disabled{opacity:.35}.vs-timeline-content{position:relative;min-width:calc(100% * var(--vs-zoom));min-height:100%}.vs-timeline-content>.vs-ruler{min-width:100%}.vs-lane .vs-clip{position:absolute;top:9px;bottom:9px;height:auto;cursor:grab;touch-action:none}.vs-lane .vs-clip:active{cursor:grabbing}.vs-clip.selected{outline:2px solid #f8fafc;outline-offset:1px;box-shadow:0 0 0 4px rgba(139,92,246,.28)}.vs-track-heads article.selected{background:rgba(139,92,246,.12)}.vs-trim{display:none;position:absolute;top:0;bottom:0;width:10px;border:0;background:rgba(255,255,255,.85);z-index:6;cursor:ew-resize}.vs-clip.selected .vs-trim{display:block}.vs-trim.left{left:0;border-radius:6px 0 0 6px}.vs-trim.right{right:0;border-radius:0 6px 6px 0}.vs-selection-info{margin-left:auto;color:#c4b5fd;font-size:11px}.vs-live-clip{position:absolute;top:9px;bottom:9px;height:auto}.vs-timeline-content .vs-empty{inset:42px 0 0}@media(max-width:1100px){.vs-edit-tools{order:4;width:100%;justify-content:center}.vs-transport{height:auto;min-height:68px;flex-wrap:wrap;padding:8px 12px}.vs-project{min-width:130px}}`;
const INSTRUMENTS=[['piano','Piano'],['electric','Piano elétrico'],['organ','Órgão'],['pad','Pad'],['strings','Strings']] as const;

function timeLabel(seconds:number){const m=Math.floor(seconds/60);const s=Math.floor(seconds%60);const ms=Math.floor((seconds%1)*10);return `${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}.${ms}`;}
function makePeaks(data:Float32Array,count=180){const step=Math.max(1,Math.floor(data.length/count));return Array.from({length:count},(_,i)=>{let max=0;for(let j=i*step;j<Math.min(data.length,(i+1)*step);j++)max=Math.max(max,Math.abs(data[j]));return Math.max(.03,max);});}
function midiFrequency(note:number){return 440*Math.pow(2,(note-69)/12);}
function instrumentWave(instrument:string):OscillatorType{return instrument==='organ'?'square':instrument==='strings'||instrument==='pad'?'sawtooth':instrument==='electric'?'triangle':'sine';}
function cloneTracks(tracks:Track[]){return tracks.map(track=>({...track,peaks:[...track.peaks],notes:track.notes.map(note=>({...note}))}));}
function typingTarget(target:EventTarget|null){return target instanceof HTMLElement&&Boolean(target.closest('input,textarea,select,[contenteditable="true"]'));}

export default function VoiceStudioDaw({readOnly}:{readOnly:boolean}){
  const [tracks,setTracks]=useState<Track[]>([]);
  const [status,setStatus]=useState<Status>('idle');
  const [tempo,setTempo]=useState(90);
  const [countInBars,setCountInBars]=useState(1);
  const [metroDuring,setMetroDuring]=useState(true);
  const [countBeat,setCountBeat]=useState(0);
  const [elapsed,setElapsed]=useState(0);
  const [meter,setMeter]=useState(0);
  const [error,setError]=useState('');
  const [trackMenu,setTrackMenu]=useState(false);
  const [armed,setArmed]=useState<ArmedTrack>({kind:'audio',instrument:'piano'});
  const [midiInputs,setMidiInputs]=useState<any[]>([]);
  const [midiInputId,setMidiInputId]=useState('');
  const [midiSupported,setMidiSupported]=useState(true);
  const [selectedIds,setSelectedIds]=useState<Set<string>>(new Set());
  const [snap,setSnap]=useState(true);
  const [zoom,setZoom]=useState(1);
  const [history,setHistory]=useState<Track[][]>([]);
  const [future,setFuture]=useState<Track[][]>([]);
  const recorderRef=useRef<MediaRecorder|null>(null);
  const chunksRef=useRef<Blob[]>([]);
  const streamRef=useRef<MediaStream|null>(null);
  const contextRef=useRef<AudioContext|null>(null);
  const analyserRef=useRef<AnalyserNode|null>(null);
  const rafRef=useRef<number|null>(null);
  const timerRef=useRef<number|null>(null);
  const metroRef=useRef<number|null>(null);
  const playAudiosRef=useRef<HTMLAudioElement[]>([]);
  const playbackTimersRef=useRef<number[]>([]);
  const scheduledNodesRef=useRef<Array<OscillatorNode|GainNode>>([]);
  const startAtRef=useRef(0);
  const playbackOffsetRef=useRef(0);
  const recordStartRef=useRef(0);
  const livePeaksRef=useRef<number[]>([]);
  const [livePeaks,setLivePeaks]=useState<number[]>([]);
  const midiAccessRef=useRef<any>(null);
  const activeMidiRef=useRef<Map<number,{start:number;velocity:number}>>(new Map());
  const midiNotesRef=useRef<MidiNote[]>([]);
  const liveOscRef=useRef<Map<number,{osc:OscillatorNode;gain:GainNode}>>(new Map());
  const dragRef=useRef<DragState|null>(null);

  const beatSeconds=60/tempo;
  const projectDuration=Math.max(8,...tracks.map(t=>t.start+t.duration),elapsed);
  const soloed=tracks.some(t=>t.solo);
  const playhead=Math.min(100,(elapsed/projectDuration)*100);
  const selected=tracks.filter(track=>selectedIds.has(track.id));

  useEffect(()=>()=>cleanup(),[]);
  useEffect(()=>{if(status!=='recording'&&status!=='countin')stopMetronome();},[status]);
  useEffect(()=>{void connectMidi();},[]);
  useEffect(()=>{bindMidiInput();return unbindMidiInput;},[midiInputId,status,armed.kind,armed.instrument]);
  useEffect(()=>{
    const keydown=(event:KeyboardEvent)=>{
      if(typingTarget(event.target))return;
      const mod=event.ctrlKey||event.metaKey;
      if(event.code==='Space'){event.preventDefault();if(status!=='recording'&&status!=='countin')playAll();return;}
      if(event.key.toLowerCase()==='r'&&!mod){event.preventDefault();if(status==='recording')stopRecording();else void beginRecord();return;}
      if(event.key.toLowerCase()==='s'&&!mod){event.preventDefault();splitSelected();return;}
      if((event.key==='Delete'||event.key==='Backspace')&&selectedIds.size){event.preventDefault();deleteSelected();return;}
      if(mod&&event.key.toLowerCase()==='d'){event.preventDefault();duplicateSelected();return;}
      if(mod&&event.key.toLowerCase()==='z'&&!event.shiftKey){event.preventDefault();undo();return;}
      if((mod&&event.key.toLowerCase()==='y')||(mod&&event.shiftKey&&event.key.toLowerCase()==='z')){event.preventDefault();redo();return;}
      if(event.key==='Escape')setSelectedIds(new Set());
    };
    window.addEventListener('keydown',keydown,true);return()=>window.removeEventListener('keydown',keydown,true);
  },[status,tracks,selectedIds,history,future,elapsed]);

  function quantize(value:number){if(!snap)return Math.max(0,value);const unit=beatSeconds/2;return Math.max(0,Math.round(value/unit)*unit);}
  function commit(mutator:(current:Track[])=>Track[]){setTracks(current=>{const next=mutator(current);if(next===current)return current;setHistory(items=>[...items.slice(-49),cloneTracks(current)]);setFuture([]);return next;});}
  function undo(){if(!history.length||status!=='idle')return;const previous=history[history.length-1];setHistory(items=>items.slice(0,-1));setFuture(items=>[cloneTracks(tracks),...items.slice(0,49)]);setTracks(cloneTracks(previous));setSelectedIds(new Set());}
  function redo(){if(!future.length||status!=='idle')return;const next=future[0];setFuture(items=>items.slice(1));setHistory(items=>[...items.slice(-49),cloneTracks(tracks)]);setTracks(cloneTracks(next));setSelectedIds(new Set());}

  function audioContext(){contextRef.current ||= new AudioContext({latencyHint:'interactive'});return contextRef.current;}
  function click(accent=false){const ctx=audioContext();const osc=ctx.createOscillator();const gain=ctx.createGain();osc.frequency.value=accent?1320:930;gain.gain.setValueAtTime(.16,ctx.currentTime);gain.gain.exponentialRampToValueAtTime(.001,ctx.currentTime+.055);osc.connect(gain).connect(ctx.destination);osc.start();osc.stop(ctx.currentTime+.06);}
  function startMetronome(){stopMetronome();click(true);let beat=1;metroRef.current=window.setInterval(()=>{click(beat%4===0);beat++;},beatSeconds*1000);}
  function stopMetronome(){if(metroRef.current){window.clearInterval(metroRef.current);metroRef.current=null;}}
  function clearPlayback(reset=false){playbackTimersRef.current.forEach(id=>window.clearTimeout(id));playbackTimersRef.current=[];playAudiosRef.current.forEach(a=>{a.pause();a.src='';});playAudiosRef.current=[];scheduledNodesRef.current.forEach(node=>{try{if('stop' in node)(node as OscillatorNode).stop();node.disconnect();}catch{}});scheduledNodesRef.current=[];if(reset)setElapsed(0);}
  function cleanup(){if(timerRef.current)window.clearInterval(timerRef.current);if(metroRef.current)window.clearInterval(metroRef.current);if(rafRef.current)cancelAnimationFrame(rafRef.current);streamRef.current?.getTracks().forEach(t=>t.stop());clearPlayback();unbindMidiInput();void contextRef.current?.close().catch(()=>undefined);}

  async function connectMidi(){const nav=navigator as Navigator&{requestMIDIAccess?:()=>Promise<any>};if(!nav.requestMIDIAccess){setMidiSupported(false);return;}try{const access=await nav.requestMIDIAccess();midiAccessRef.current=access;const load=()=>{const inputs=Array.from(access.inputs.values()) as any[];setMidiInputs(inputs);setMidiInputId(current=>current||inputs[0]?.id||'');};load();access.onstatechange=load;}catch{setError('Permita o acesso MIDI no navegador para usar o teclado.');}}
  function selectedMidiInput(){return midiInputs.find(input=>input.id===midiInputId);}
  function bindMidiInput(){const input=selectedMidiInput();if(input)input.onmidimessage=handleMidiMessage;}
  function unbindMidiInput(){midiInputs.forEach(input=>{if(input.onmidimessage===handleMidiMessage)input.onmidimessage=null;});}
  function handleMidiMessage(event:any){const [command,note,velocity]=event.data as [number,number,number];const type=command&0xf0;const noteOn=type===0x90&&velocity>0;const noteOff=type===0x80||(type===0x90&&velocity===0);if(noteOn){playLiveNote(note,velocity,armed.instrument);if(status==='recording'&&armed.kind==='midi')activeMidiRef.current.set(note,{start:(performance.now()-startAtRef.current)/1000,velocity});}if(noteOff){stopLiveNote(note);const active=activeMidiRef.current.get(note);if(active&&status==='recording'&&armed.kind==='midi'){midiNotesRef.current.push({id:crypto.randomUUID(),note,velocity:active.velocity,start:active.start,duration:Math.max(.04,(performance.now()-startAtRef.current)/1000-active.start)});activeMidiRef.current.delete(note);}}}
  function playLiveNote(note:number,velocity:number,instrument:string){const ctx=audioContext();void ctx.resume();stopLiveNote(note);const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type=instrumentWave(instrument);osc.frequency.value=midiFrequency(note);gain.gain.setValueAtTime(0,ctx.currentTime);gain.gain.linearRampToValueAtTime(Math.max(.025,(velocity/127)*.18),ctx.currentTime+.012);osc.connect(gain).connect(ctx.destination);osc.start();liveOscRef.current.set(note,{osc,gain});}
  function stopLiveNote(note:number){const voice=liveOscRef.current.get(note);if(!voice)return;const ctx=audioContext();voice.gain.gain.cancelScheduledValues(ctx.currentTime);voice.gain.gain.setTargetAtTime(.0001,ctx.currentTime,.04);voice.osc.stop(ctx.currentTime+.2);liveOscRef.current.delete(note);}

  async function countIn(){const totalBeats=countInBars*4;if(totalBeats<=0)return;setStatus('countin');setCountBeat(1);click(true);let beat=1;await new Promise<void>(resolve=>{const id=window.setInterval(()=>{beat++;if(beat>totalBeats){window.clearInterval(id);resolve();return;}setCountBeat(beat);click((beat-1)%4===0);},beatSeconds*1000);});}
  async function beginRecord(){if(readOnly||status!=='idle')return;setError('');recordStartRef.current=elapsed>=projectDuration?0:elapsed;playbackOffsetRef.current=recordStartRef.current;setLivePeaks([]);livePeaksRef.current=[];midiNotesRef.current=[];activeMidiRef.current.clear();try{await audioContext().resume();if(armed.kind==='audio')await prepareAudio();else if(!selectedMidiInput())throw new Error('Conecte e selecione um teclado MIDI.');await countIn();startBackingTracks(recordStartRef.current);if(armed.kind==='audio')startAudioRecorder();else startMidiRecorder();}catch(reason){setStatus('idle');setError(reason instanceof Error?reason.message:'Não foi possível iniciar a gravação.');cleanupCapture();}}
  async function prepareAudio(){const deviceId=localStorage.getItem('foco-live-microphone-device');const stream=await navigator.mediaDevices.getUserMedia({audio:{deviceId:deviceId?{exact:deviceId}:undefined,echoCancellation:false,noiseSuppression:false,autoGainControl:false}});streamRef.current=stream;const ctx=audioContext();const source=ctx.createMediaStreamSource(stream);const analyser=ctx.createAnalyser();analyser.fftSize=512;source.connect(analyser);analyserRef.current=analyser;watchInput();}
  function startClock(){startAtRef.current=performance.now();setElapsed(recordStartRef.current);setStatus('recording');if(metroDuring)startMetronome();timerRef.current=window.setInterval(()=>setElapsed(recordStartRef.current+(performance.now()-startAtRef.current)/1000),50);}
  function startAudioRecorder(){const stream=streamRef.current;if(!stream)return;const recorder=new MediaRecorder(stream);recorderRef.current=recorder;chunksRef.current=[];recorder.ondataavailable=e=>{if(e.data.size)chunksRef.current.push(e.data);};recorder.onstop=()=>void finishAudio(recorder);recorder.start(100);startClock();}
  function startMidiRecorder(){startClock();}
  function watchInput(){const analyser=analyserRef.current;if(!analyser)return;const data=new Uint8Array(analyser.frequencyBinCount);const draw=()=>{analyser.getByteTimeDomainData(data);let sum=0,max=0;for(const v of data){const n=Math.abs((v-128)/128);sum+=n*n;max=Math.max(max,n);}setMeter(Math.min(1,Math.sqrt(sum/data.length)*3));if(recorderRef.current?.state==='recording'){livePeaksRef.current.push(Math.max(.03,max));if(livePeaksRef.current.length>220)livePeaksRef.current.shift();setLivePeaks([...livePeaksRef.current]);}rafRef.current=requestAnimationFrame(draw);};draw();}
  function cleanupCapture(){if(timerRef.current)window.clearInterval(timerRef.current);timerRef.current=null;stopMetronome();clearPlayback();if(rafRef.current)cancelAnimationFrame(rafRef.current);rafRef.current=null;streamRef.current?.getTracks().forEach(t=>t.stop());streamRef.current=null;setMeter(0);}
  function stopRecording(){if(armed.kind==='audio'&&recorderRef.current?.state==='recording')recorderRef.current.stop();else if(armed.kind==='midi')finishMidi();}
  async function finishAudio(recorder:MediaRecorder){const duration=(performance.now()-startAtRef.current)/1000;const blob=new Blob(chunksRef.current,{type:recorder.mimeType||'audio/webm'});const url=URL.createObjectURL(blob);let peaks=livePeaksRef.current;try{const buffer=await audioContext().decodeAudioData(await blob.arrayBuffer());peaks=makePeaks(buffer.getChannelData(0));}catch{}addTrack({kind:'audio',url,blob,start:recordStartRef.current,sourceOffset:0,duration,sourceDuration:duration,peaks,notes:[],instrument:'',name:`Voz ${tracks.filter(t=>t.kind==='audio').length+1}`});cleanupCapture();setElapsed(recordStartRef.current);setStatus('idle');}
  function finishMidi(){const duration=(performance.now()-startAtRef.current)/1000;activeMidiRef.current.forEach((active,note)=>midiNotesRef.current.push({id:crypto.randomUUID(),note,velocity:active.velocity,start:active.start,duration:Math.max(.04,duration-active.start)}));activeMidiRef.current.clear();addTrack({kind:'midi',start:recordStartRef.current,sourceOffset:0,duration,sourceDuration:duration,peaks:[],notes:[...midiNotesRef.current],instrument:armed.instrument,name:`Teclado ${tracks.filter(t=>t.kind==='midi').length+1}`});cleanupCapture();setElapsed(recordStartRef.current);setStatus('idle');}
  function addTrack(data:Omit<Track,'id'|'color'|'muted'|'solo'|'volume'>){commit(current=>[...current,{id:crypto.randomUUID(),color:COLORS[current.length%COLORS.length],muted:false,solo:false,volume:1,...data}]);}

  function playableTracks(){return tracks.filter(t=>!t.muted&&(!soloed||t.solo));}
  function startBackingTracks(offset:number){clearPlayback();const ctx=audioContext();const playable=playableTracks();playable.filter(t=>t.kind==='audio'&&t.url&&offset<t.start+t.duration).forEach(track=>{const delay=Math.max(0,track.start-offset);const sourceTime=track.sourceOffset+Math.max(0,offset-track.start);if(sourceTime>=track.sourceOffset+track.duration)return;const audio=new Audio(track.url);audio.volume=track.volume;audio.currentTime=sourceTime;playAudiosRef.current.push(audio);const start=()=>{void audio.play();const remaining=Math.max(.01,track.duration-Math.max(0,offset-track.start));playbackTimersRef.current.push(window.setTimeout(()=>audio.pause(),remaining*1000));};if(delay>.005)playbackTimersRef.current.push(window.setTimeout(start,delay*1000));else start();});playable.filter(t=>t.kind==='midi').forEach(track=>scheduleMidiTrack(track,ctx.currentTime,offset));}
  function playAll(){if(status==='playing'){stopPlayback();return;}if(!tracks.length)return;const offset=elapsed>=projectDuration?0:elapsed;playbackOffsetRef.current=offset;void audioContext().resume();startBackingTracks(offset);startAtRef.current=performance.now();setStatus('playing');timerRef.current=window.setInterval(()=>{const next=playbackOffsetRef.current+(performance.now()-startAtRef.current)/1000;setElapsed(next);if(next>=projectDuration)stopPlayback(true);},50);}
  function scheduleMidiTrack(track:Track,base:number,offset=0){const ctx=audioContext();track.notes.forEach(note=>{const globalStart=track.start+note.start;const globalEnd=globalStart+note.duration;if(globalEnd<=offset||globalStart>=track.start+track.duration)return;const start=base+Math.max(0,globalStart-offset);const remaining=Math.min(globalEnd,track.start+track.duration)-Math.max(offset,globalStart);const end=start+remaining;const osc=ctx.createOscillator();const gain=ctx.createGain();osc.type=instrumentWave(track.instrument);osc.frequency.value=midiFrequency(note.note);gain.gain.setValueAtTime(0,start);gain.gain.linearRampToValueAtTime((note.velocity/127)*.16*track.volume,start+.01);gain.gain.setTargetAtTime(.0001,end,.04);osc.connect(gain).connect(ctx.destination);osc.start(start);osc.stop(end+.2);scheduledNodesRef.current.push(osc,gain);});}
  function stopPlayback(reset=false){if(timerRef.current)window.clearInterval(timerRef.current);timerRef.current=null;clearPlayback(reset);setStatus('idle');}
  function seekTimeline(event:React.MouseEvent<HTMLElement>){if(status!=='idle'||(event.target as HTMLElement).closest('.vs-clip'))return;const rect=event.currentTarget.getBoundingClientRect();const ratio=Math.min(1,Math.max(0,(event.clientX-rect.left+event.currentTarget.scrollLeft)/(event.currentTarget.scrollWidth)));setElapsed(quantize(ratio*projectDuration));setSelectedIds(new Set());}
  function patch(id:string,value:Partial<Track>){commit(current=>current.map(t=>t.id===id?{...t,...value}:t));}
  function selectClip(event:React.PointerEvent,id:string){event.stopPropagation();if(event.ctrlKey||event.metaKey||event.shiftKey)setSelectedIds(current=>{const next=new Set(current);if(next.has(id))next.delete(id);else next.add(id);return next;});else setSelectedIds(new Set([id]));}
  function beginDrag(event:React.PointerEvent,id:string,mode:EditMode){if(readOnly||status!=='idle')return;event.preventDefault();event.stopPropagation();const track=tracks.find(item=>item.id===id);const timeline=(event.currentTarget as HTMLElement).closest('.vs-timeline') as HTMLElement|null;if(!track||!timeline)return;if(!selectedIds.has(id))setSelectedIds(new Set([id]));dragRef.current={id,mode,startX:event.clientX,initial:{...track},timelineWidth:timeline.scrollWidth};(event.currentTarget as HTMLElement).setPointerCapture(event.pointerId);}
  function moveDrag(event:React.PointerEvent){const drag=dragRef.current;if(!drag)return;const delta=(event.clientX-drag.startX)/drag.timelineWidth*projectDuration;commitPreview(drag,delta);}
  function commitPreview(drag:DragState,delta:number){setTracks(current=>current.map(track=>{if(track.id!==drag.id)return track;if(drag.mode==='move')return {...track,start:quantize(drag.initial.start+delta)};if(drag.mode==='trim-right')return {...track,duration:Math.max(MIN_CLIP,quantize(drag.initial.duration+delta))};const maxDelta=drag.initial.duration-MIN_CLIP;const applied=Math.min(maxDelta,Math.max(-drag.initial.sourceOffset,quantize(drag.initial.start+delta)-drag.initial.start));return {...track,start:drag.initial.start+applied,sourceOffset:drag.initial.sourceOffset+applied,duration:drag.initial.duration-applied};}));}
  function endDrag(){const drag=dragRef.current;if(!drag)return;dragRef.current=null;setHistory(items=>[...items.slice(-49),cloneTracks(tracks.map(track=>track.id===drag.id?drag.initial:track))]);setFuture([]);}
  function deleteSelected(){if(readOnly||status!=='idle'||!selectedIds.size)return;commit(current=>current.filter(track=>!selectedIds.has(track.id)));setSelectedIds(new Set());}
  function duplicateSelected(){if(readOnly||status!=='idle'||!selected.length)return;const offset=snap?beatSeconds:0.25;const clones=selected.map((track,index)=>({...track,id:crypto.randomUUID(),name:`${track.name} cópia`,start:track.start+offset,color:COLORS[(tracks.length+index)%COLORS.length],notes:track.notes.map(note=>({...note,id:crypto.randomUUID()}))}));commit(current=>[...current,...clones]);setSelectedIds(new Set(clones.map(track=>track.id)));}
  function splitSelected(){if(readOnly||status!=='idle'||!selected.length)return;const replacements:Track[]=[];const ids=new Set(selected.map(track=>track.id));selected.forEach(track=>{const local=elapsed-track.start;if(local<=MIN_CLIP||local>=track.duration-MIN_CLIP)return;const leftNotes=track.notes.filter(note=>note.start<local).map(note=>({...note,duration:Math.min(note.duration,Math.max(.04,local-note.start))}));const rightNotes=track.notes.filter(note=>note.start+note.duration>local).map(note=>({...note,id:crypto.randomUUID(),start:Math.max(0,note.start-local),duration:Math.min(note.duration,note.start+note.duration-local)}));replacements.push({...track,id:crypto.randomUUID(),duration:local,notes:leftNotes},{...track,id:crypto.randomUUID(),name:`${track.name} B`,start:elapsed,sourceOffset:track.sourceOffset+local,duration:track.duration-local,notes:rightNotes});});if(!replacements.length)return;commit(current=>[...current.filter(track=>!ids.has(track.id)),...replacements]);setSelectedIds(new Set(replacements.map(track=>track.id)));}
  function exportTracks(){tracks.forEach(t=>{if(t.kind==='audio'&&t.url){download(t.url,`${slug(t.name)}.webm`);}else if(t.kind==='midi'){const blob=createMidiFile(t.notes,tempo);download(URL.createObjectURL(blob),`${slug(t.name)}.mid`,true);}});}
  function download(url:string,name:string,revoke=false){const a=document.createElement('a');a.href=url;a.download=name;a.click();if(revoke)setTimeout(()=>URL.revokeObjectURL(url),1000);}
  function slug(value:string){return value.replace(/\s+/g,'-').toLowerCase();}
  function selectTrack(kind:TrackKind){setArmed(current=>({...current,kind}));setTrackMenu(false);}

  const ruler=useMemo(()=>Array.from({length:BARS},(_,i)=>i+1),[]);
  return <div className="vs-daw" style={{'--vs-zoom':zoom} as React.CSSProperties}>
    <style>{EDITOR_CSS}</style>
    <header className="vs-transport"><div className="vs-project"><strong>Voice Studio</strong><span>DAW vocal multipista</span></div><div className="vs-tempo"><input disabled={readOnly||status!=='idle'} value={tempo} min={40} max={220} type="number" onChange={e=>setTempo(Number(e.target.value)||90)}/><span>BPM</span><b>4 / 4</b></div><div className="vs-edit-tools"><button title="Desfazer" disabled={!history.length||status!=='idle'} onClick={undo}><Undo2/></button><button title="Refazer" disabled={!future.length||status!=='idle'} onClick={redo}><Redo2/></button><button title="Dividir no playhead (S)" disabled={!selected.length||status!=='idle'} onClick={splitSelected}><Scissors/></button><button title="Duplicar (Ctrl/Cmd + D)" disabled={!selected.length||status!=='idle'} onClick={duplicateSelected}><Copy/></button><button className={snap?'active':''} title="Snap" onClick={()=>setSnap(value=>!value)}><Magnet/></button><button title="Reduzir zoom" onClick={()=>setZoom(value=>Math.max(1,value-.25))}><ZoomOut/></button><button title="Aumentar zoom" onClick={()=>setZoom(value=>Math.min(4,value+.25))}><ZoomIn/></button></div><div className="vs-main-controls"><button onClick={playAll} disabled={!tracks.length||status==='recording'||status==='countin'}>{status==='playing'?<Pause/>:<Play/>}</button><button className={status==='recording'?'recording':'record'} onClick={status==='recording'?stopRecording:beginRecord} disabled={readOnly||status==='countin'||status==='playing'}>{status==='recording'?<Square/>:<Circle fill="currentColor"/>}</button><time>{timeLabel(elapsed)}</time></div>{!readOnly&&<button className="vs-export" disabled={!tracks.length} onClick={exportTracks}><Download/> Exportar</button>}</header>
    <section className="vs-options"><label>Contagem<select disabled={status!=='idle'||readOnly} value={countInBars} onChange={e=>setCountInBars(Number(e.target.value))}><option value={0}>Sem contagem</option><option value={1}>1 compasso</option><option value={2}>2 compassos</option></select></label><button className={metroDuring?'active':''} disabled={status!=='idle'||readOnly} onClick={()=>setMetroDuring(v=>!v)}>Metrônomo durante a gravação</button>{armed.kind==='midi'?<><label>Teclado<select disabled={status!=='idle'||readOnly} value={midiInputId} onChange={e=>setMidiInputId(e.target.value)}><option value="">Selecione</option>{midiInputs.map(input=><option key={input.id} value={input.id}>{input.name||'Teclado MIDI'}</option>)}</select></label><label>Timbre<select disabled={status!=='idle'||readOnly} value={armed.instrument} onChange={e=>setArmed(v=>({...v,instrument:e.target.value}))}>{INSTRUMENTS.map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label></>:<div className="vs-input"><Mic2/><span>Nível de entrada</span><i><b style={{width:`${meter*100}%`}}/></i></div>}{selected.length>0&&<strong className="vs-selection-info">{selected.length} clip{selected.length>1?'s':''} selecionado{selected.length>1?'s':''}</strong>}{error&&<em>{error}</em>}{!midiSupported&&<em>Este navegador não oferece Web MIDI.</em>}</section>
    <div className="vs-editor"><aside className="vs-track-heads"><div className="vs-add-wrap"><button className="vs-add" onClick={()=>setTrackMenu(v=>!v)} disabled={readOnly||status!=='idle'}><Plus/> ADICIONAR FAIXA <ChevronDown/></button>{trackMenu&&<div className="vs-track-menu"><button onClick={()=>selectTrack('audio')}><AudioLines/><div><b>Voz / Áudio</b><small>Gravação pelo microfone</small></div></button><button disabled={!midiSupported} onClick={()=>selectTrack('midi')}><KeyboardMusic/><div><b>Teclado MIDI</b><small>Notas, velocity e sustain</small></div></button></div>}</div>{tracks.map((t,i)=><article key={t.id} className={selectedIds.has(t.id)?'selected':''} style={{'--track':t.color} as React.CSSProperties}><span>{t.kind==='midi'?<KeyboardMusic/>:String(i+1).padStart(2,'0')}</span><input disabled={readOnly} value={t.name} onChange={e=>patch(t.id,{name:e.target.value})}/><div><button className={t.muted?'active':''} disabled={readOnly} onClick={()=>patch(t.id,{muted:!t.muted})}>M</button><button className={t.solo?'solo':''} disabled={readOnly} onClick={()=>patch(t.id,{solo:!t.solo})}>S</button>{!readOnly&&<button onClick={()=>{commit(current=>current.filter(track=>track.id!==t.id));setSelectedIds(current=>{const next=new Set(current);next.delete(t.id);return next;});}}><Trash2/></button>}</div><label><Volume2/><input disabled={readOnly} type="range" min="0" max="1" step=".05" value={t.volume} onChange={e=>patch(t.id,{volume:Number(e.target.value)})}/></label></article>)}{(status==='recording'||status==='countin')&&<article className="armed"><span>●</span><strong>{armed.kind==='midi'?'Nova faixa MIDI':'Nova voz'}</strong><small>{status==='countin'?'Preparando…':'GRAVANDO'}</small></article>}</aside><main className="vs-timeline" onClick={seekTimeline}><div className="vs-timeline-content"><div className="vs-ruler">{ruler.map(n=><span key={n}>{n}</span>)}</div><div className="vs-playhead" style={{left:`${playhead}%`}}/>{tracks.map(t=><div className={`vs-lane ${t.kind}`} key={t.id}><div className={`vs-clip ${selectedIds.has(t.id)?'selected':''}`} onPointerDown={event=>{selectClip(event,t.id);beginDrag(event,t.id,'move');}} onPointerMove={moveDrag} onPointerUp={endDrag} onPointerCancel={endDrag} style={{'--clip':t.color,left:`${(t.start/projectDuration)*100}%`,width:`${Math.max(.8,(t.duration/projectDuration)*100)}%`} as React.CSSProperties}><button className="vs-trim left" aria-label="Aparar início" onPointerDown={event=>beginDrag(event,t.id,'trim-left')} onPointerMove={moveDrag} onPointerUp={endDrag}/><b>{t.name}</b>{t.kind==='audio'?<Wave peaks={t.peaks} offset={t.sourceOffset} duration={t.duration} sourceDuration={t.sourceDuration}/>:<MidiClip notes={t.notes} duration={t.duration}/>}<button className="vs-trim right" aria-label="Aparar final" onPointerDown={event=>beginDrag(event,t.id,'trim-right')} onPointerMove={moveDrag} onPointerUp={endDrag}/></div></div>)}{(status==='recording'||status==='countin')&&<div className={`vs-lane live ${armed.kind}`}><div className="vs-live-clip" style={{left:`${(recordStartRef.current/projectDuration)*100}%`,width:`${Math.max(1,((elapsed-recordStartRef.current)/projectDuration)*100)}%`}}>{armed.kind==='audio'?<Wave peaks={livePeaks}/>:<div className="vs-midi-live"><KeyboardMusic/><span>Capturando MIDI…</span></div>}</div></div>}{!tracks.length&&status==='idle'&&<div className="vs-empty">{armed.kind==='midi'?<KeyboardMusic/>:<Mic2/>}<strong>{armed.kind==='midi'?'Grave seu teclado MIDI':'Grave a voz principal'}</strong><span>Depois selecione, mova, apare, divida e duplique os clips na timeline.</span><button onClick={event=>{event.stopPropagation();void beginRecord();}} disabled={readOnly}><Circle fill="currentColor"/> Criar primeira faixa</button></div>}</div></main></div>
    {status==='countin'&&<div className="vs-countin"><small>ENTRADA EM</small><strong>{((countBeat-1)%4)+1}</strong><div>{Array.from({length:4},(_,i)=><i key={i} className={i===((countBeat-1)%4)?'active':''}/>)}</div><span>Compasso {Math.ceil(countBeat/4)} de {countInBars}</span></div>}
  </div>;
}

function Wave({peaks,offset=0,duration,sourceDuration}:{peaks:number[];offset?:number;duration?:number;sourceDuration?:number}){const values=peaks.length?peaks:Array.from({length:80},()=>.04);const total=Math.max(.01,sourceDuration||duration||1);const start=Math.floor(offset/total*values.length);const end=Math.max(start+1,Math.ceil((offset+(duration||total))/total*values.length));const visible=values.slice(start,end);return <svg className="vs-wave" viewBox={`0 0 ${Math.max(1,visible.length)} 100`} preserveAspectRatio="none">{visible.map((p,i)=><line key={i} x1={i+.5} x2={i+.5} y1={50-p*46} y2={50+p*46}/>)}</svg>;}
function MidiClip({notes,duration}:{notes:MidiNote[];duration:number}){return <div className="vs-midi-notes">{notes.map(note=>{const top=((84-Math.min(84,Math.max(36,note.note)))/48)*100;return <i key={note.id} style={{left:`${(note.start/Math.max(.1,duration))*100}%`,width:`${Math.max(1.2,(note.duration/Math.max(.1,duration))*100)}%`,top:`${top}%`,opacity:.45+(note.velocity/127)*.55}}/>})}</div>;}
function variableLength(value:number){const bytes=[value&0x7f];while((value>>=7))bytes.unshift((value&0x7f)|0x80);return bytes;}
function createMidiFile(notes:MidiNote[],tempo:number){const ppq=480;const events:Array<{tick:number;data:number[]}>=[];notes.forEach(note=>{events.push({tick:Math.round(note.start*tempo/60*ppq),data:[0x90,note.note,note.velocity]});events.push({tick:Math.round((note.start+note.duration)*tempo/60*ppq),data:[0x80,note.note,0]});});events.sort((a,b)=>a.tick-b.tick);const track:number[]=[];let last=0;const mpqn=Math.round(60000000/tempo);track.push(0,0xff,0x51,3,(mpqn>>16)&255,(mpqn>>8)&255,mpqn&255);events.forEach(event=>{track.push(...variableLength(event.tick-last),...event.data);last=event.tick;});track.push(0,0xff,0x2f,0);const header=[0x4d,0x54,0x68,0x64,0,0,0,6,0,0,0,1,(ppq>>8)&255,ppq&255];const length=track.length;const chunk=[0x4d,0x54,0x72,0x6b,(length>>>24)&255,(length>>>16)&255,(length>>>8)&255,length&255,...track];return new Blob([new Uint8Array([...header,...chunk])],{type:'audio/midi'});}
